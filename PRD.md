# PRD — TINN 시멘트 수화 4D 플랫폼 v2 (구현 우선 재작성)

문서 버전: 4.0-RT / 작성일: 2026-07-14 / 개정: 2026-07-15 (v2.1 SCM, v2.2 전평형 재용해·재료별 입자군·공극 분석), 2026-07-16 (rev.2: 겔 전도 용해 접근 §4.2, 공간 충전 동결 §4.5, 배치 오버플로 §2.2, 기본 번들 CNASH(InverseGems Test) 채택 §1.2/§6.2, 2-스케일 공극 분석 — 네트워크 D_eff·서브복셀 크기분포 §1.3, 재료별 타원체 입자 형상 §1.2), 2026-07-17b (실측 PSD 입력 — 누적곡선/Rosin-Rammler·격자 절단·SSA 진단 §1.2), 2026-07-30 (v3.0/E1: 고용체 endmember 원장 §2.3·폐합 2종 §6.1·파생 관측치 §1.3, FORMAT_VERSION 3 + RT-W2 경계 원장 예약, 테스트 예산 220 — 계획서 `TINN v2 report\planning\endmember-preservation-plan.md`), 2026-07-30b (v3.0/E2: 클러스터별 endmember 풀 `cluster_endmember_mol` §2.3 — 급이 조성을 자기 풀 covered-portion으로, 글로벌 평균은 미커버 질량 fallback으로 강등 §4.5, endmember 비음수 1b(c) §6.1, 클러스터 해상도 Ca/Si 관측치(지배 규산 채널 단일 선택)·지도 §1.3; FORMAT_VERSION 3 동승 — E2 리뷰 게이트 12건 반영), 2026-07-30c (v3.0/E3: 황산염·알칼리 투입 채널 §1.2 — 가용성 염 담체 5상(번들 V0 실측 몰부피), 재료별 입자군, 리포트 `binder_oxides` 레시피+as-built §1.3, 화학수축·SCM 비중 분모에서 염 제외 §6.3, 테스트 예산 240 §0.4; pre-E3 체크포인트 재시작 불가·염 없는 config 해시 불변 — E3 리뷰 게이트 7건 반영), 2026-07-31 (v3.0/E3b 사용자 결정: 담체 용해를 **속도식 없이 용해도 제어**로 §1.2/§4.1/§4.2 — 매 스텝 물이 닿는 전량을 평형에 내놓고 GEMS가 고체 잔량을 결정, 1차 방출 `SALT_TAU_H_PRESETS`·`kinetics.salt_tau_h` 삭제(근거 없는 수치), tabulated 표의 담체 열은 거부), 2026-08-20 (v4.0/RT: 반응-수송 확장 —
저장소 분기 `TINN_ reactive transport`(f7be88b에서 clone, 브랜치 rt-dev). 두 직교 모드를
config 선택형으로 도입: **모드 B** 속도제한 재평형(교환시간 τ — 스텝당 소유 고용체의
f=min(1,Δt/τ)만 평형에 제공) §4.6.1, **모드 C** 서브클러스터 평형 도메인 + 도메인 그래프
암시적 확산 §4.6.2. §1.4 셀간 수송 제외 일부 해제(도메인 그래프 한정 — per-voxel FVM은
여전히 제외), transport.py 확장(§2.1 16/16 유지), 테스트 예산 240→280·파일 14→15 §0.4,
FORMAT_VERSION 4는 RT-W2에서 1회 단절 §2.3, 마일스톤 RT-W0..W4 §3, 경계 저수조
필드 라벨 RT-W2→RT-W3 정정. 계획서: 세션 플랜 문서, 설계 3관점 종합)
목적: 이 문서 하나만으로 Claude(Fable)가 빈 저장소에서 플랫폼 전체를 처음부터 재구현할 수 있어야 한다.
최상위 원칙: **구현이 우선이다.** 검증은 각 마일스톤의 "완료 조건"에 명시된 최소 세트만 수행하고, 그 외의 검증·증적(evidence)·QC 산출물은 만들지 않는다.

---

## 0. 배경 — v1 회고와 v2 설계 원칙

### v1에서 달성한 것 (재사용할 자산)

v1(2026-07 개발분)은 다음을 실제로 동작시켰고, 이 설계들은 v2에 그대로 계승한다:

- 주기 경계 3D 복셀 RVE에서 해상(resolved)/부분해상(fractional)/서브그리드 입자의 3계층 고체 표현.
- 트랜잭션 스테핑: copy-on-write 트라이얼 → 전체 불변식 검사 → 원자적 커밋 또는 완전 롤백.
- float64 성분/상/물/부피 원장(ledger)과 보존 오차 ~1e-25 mol 수준의 수치 폐쇄.
- 연결 액체 클러스터(6-이웃, 주기) + overlap-matrix 기반 보존적 인벤토리 리매핑.
- 의존성 없는 Zarr-v2 디렉터리 체크포인트와 비트단위 재시작 동등성.
- Parrot–Killoh 4상 동역학 2종 프리셋(Elakneswaran 2018, CemGEMS 2021)의 검증된 구현.
- xGEMS `ChemicalEngine`을 격리 프로세스에서 호출하는 어댑터와 PC(Cemdata) 번들 스모크 성공
  (재평형: pH 13.596951, I=0.366219 molal / C3S+물 스모크: pH 12.661942, CSHQ+CH 생성).
- 가변 조성 용액상(CSHQ)을 고정 화학식으로 재해석하지 않는 product-parcel 원장 개념.

### v1이 실패한 방식 (v2에서 금지)

- **모듈 폭증**: 소스 약 90개 모듈(~2.3MB), 테스트 80여 파일. 상당수가 특정 게이트 전용 일회성 모듈
  (`g6c_*`, `g7c_*`, `opc_0d_evidence`, `vendor_neutral_evidence`, `public_operator_evidence` 등).
- **검증 관료제**: evidence registry JSON, QC 정책 파일, 관측 연산자 차터 등 산출물이 코드보다 빨리 늘었다.
- **점진 마이그레이션 비용**: 스칼라 C3S alpha로 시작해 다상(phase-vector) 상태로 이행하며
  호환 뷰·스키마 마이그레이션·이중 원장이 누적됐다.
- **검증 실패가 전체를 블로킹**: XCT 세그멘테이션 QC 실패로 과학 검증이 0건인 채 코드만 확장됐다.
- **문서 부패**: README와 로드맵이 실제 코드 진행보다 수 게이트 뒤처졌다.

### v2 설계 원칙 (전 마일스톤 공통 강제 규칙)

1. **구현 우선.** 기능이 동작하는 스모크 런이 먼저다. 검증은 §6의 최소 세트만.
2. **모듈 상한.** `src/tinn/` 아래 파이썬 모듈은 **최대 16개**. 초과가 필요하면 기존 모듈을 삭제·통합한 뒤 추가한다.
   게이트 이름·마일스톤 이름을 딴 모듈 금지 (예: `g6c_*.py` 금지).
   (rev.2 주석: 현재 16/16 도달. 차기 확장인 reactive transport는 새 모듈이 아니라
   `transport.py`의 확장으로 구현한다 — 클러스터 라벨링·리매핑과 동일한 관심사다.)
   (v4.0/RT 주석: 그 약속대로 도메인 분할·도메인 그래프 확산 연산자·전도장 헬퍼를
   전부 `transport.py`에 둔다. 16/16 유지 — `reactive_transport.py` 신설 금지.)
3. **처음부터 다상(multiphase).** 상태는 첫 커밋부터 phase-vector 기반. 스칼라 alpha 전용 경로,
   호환 뷰, 스키마 마이그레이션 코드를 만들지 않는다.
4. **테스트 예산.** 전체 테스트 파일 ≤ 16개, 총 테스트 ≤ 300개. 한 기능에 한 테스트가 원칙.
   (RT-S1 개정 2026-09-02: 280 → 300, 파일 15 → 16. 실측 278/13에서 잔여
   2개로는 S1의 게이트 ~5개(널 연산자·등온선·폐합·재시작·검증기)를 담을 수
   없다 — E3 전례(실측 요구로 상향)와 같은 사유. 상한 회피용 병합 금지는
   그대로.)
   (v4.0/RT 개정: 240 → 280, 파일 14 → 15. 실측 기점 225/11파일(f7be88b);
   RT-P0b 시점 실측 278/13파일 — `tests/test_np.py` 신설 10개. RT가
   `tests/test_rt.py` 1파일 + 약 22개(모드 B 등가·폐합·재시작, 파티션·해석해 3종·
   플럭스 반대칭·결정론, 경계 저수조 폐합, config 해시 불변)를 요구하고, 플랫폼
   상류(WIP)의 커밋 합류 시 +2파일/+10개 여유를 남긴다. 우산 병합 금지는 그대로.)
   (v3.0/E3 개정: 220 → 240. E1/E2 상향 때 E3(황산염·알칼리 채널)는 계획서상
   "추가 후보"라 예산에 들어 있지 않았는데, 실제로는 상 5개·새 용해 규칙·새 진단으로
   19개를 요구했다(실측 222/220). 상한 회피용 병합은 여전히 금지이므로 예산을 올린다.)
   (v3.0 개정: E1/E2 endmember 보존이 +25~30개를 요구해 180 → 220 —
   endmember 계획서의 선행 결정. 우산 병합 금지는 그대로.)
   (rev.2 개정: 150 상한 도달 후 무관한 어서션을 기존 테스트에 병합하는 회피가 발생 —
   상한을 소폭 올리되, **상한 회피 목적의 우산 테스트 병합은 금지**한다. 상한의 목적은
   v1식 테스트 폭증 방지이지 테스트 명료성 훼손이 아니다.)
5. **문서는 2개.** 이 PRD와 README.md. 별도 아키텍처/검증계획/증적 문서를 만들지 않는다.
   결정 변경은 PRD를 직접 수정한다.
6. **검증 실패는 기록하고 전진.** 최소 검증(§6)에서 sanity band를 벗어나면 요약 리포트에 기록하되,
   수치 보존(§6.1)만 통과하면 다음 마일스톤 진행을 막지 않는다. 수치 보존 실패만 블로킹이다.
7. **git 필수.** 첫 커밋 전 `git init`. 마일스톤마다 최소 1커밋. `code_version`은 실제 git 해시.

---

## 1. 제품 정의

### 1.1 한 문장 정의

배합(순수 C3S 또는 OPC 4상 클링커), PSD, w/c, 온도, 시간 스케줄을 입력받아
**질량이 보존되는 시간 의존 3D 미세구조(상 분포·물 분배·공극)와 0D 화학 상태(GEMS 연결 시 pH·상 조성 포함)**를
결정론적·재시작 가능하게 출력하는 시뮬레이션 플랫폼.

### 1.2 입력

| 입력 | 형식 | 필수 여부 |
|---|---|---|
| 결합재 레시피 | 상별 질량분율 (C3S 단독 또는 C3S/C2S/C3A/C4AF + 미배정 잔여) | 필수 |
| PSD | **실측 입력 3형식** (rev.2, CEMHYD3D 방식): ① `bins` 로그 구간 부피분율(정준형), ② `cumulative` 레이저 회절 누적곡선 [(d_um, F)] 원표 그대로 — 측정창 [F_first, F_last]로 정규화하고 창 밖 질량은 리포트에 명시(`psd_window_excluded`), ③ `rosin_rammler` {d′, n, d_min, d_max} — 10진당 8빈 로그 이산화. 측정형이 있으면 bins는 파생 정준형으로 재계산(직렬화 라운드트립 안전). `truncate_to_grid`(기본 false=거부): 래스터 한계 초과 조대 빈을 제거·재정규화하고 제거 분율을 `psd_truncated_volume_fraction`으로 기록. geometry가 구 가정 비표면 `ssa_est_m2_kg`을 진단 출력(실측 Blaine 교차확인용). 선택적 재료별 맵(`material_psd`) 동일 적용. 기준 실측 데이터: SRM 114q 누적 PSD(NIST SP 260-166 Table 8) = `examples/opc_srm114q_measured_psd_64.json` | 필수(합성 기본값 허용) |
| 입자 형상 | 선택적 재료별 형상 맵(`material_shape`, rev.2): kind=ellipsoid, 반축비 a:b:c(부피 정규화 — PSD의 부피등가지름 의미 보존), 방향은 입자별 시드 균일 샘플링(SO(3)). 부재 시 구형 — 기존 config의 해시·RNG 스트림 비트동일 보존. 장축이 래스터 한계를 넘으면 거부 | 선택 |
| w/c (또는 w/C3S) | 질량비 | 필수 |
| 온도 | K, 등온 | 필수 |
| 동역학 | `tabulated` alpha(t) 테이블 **또는** `pk` 프리셋 이름 + Blaine(m²/kg) | 필수 |
| RVE | 격자 크기(32³/64³/128³), 복셀 크기(0.25–1.0 µm), 시드 | 필수 (128³·0.25 µm는 해상도 수렴 트랙 — rev.2; 0.1 µm 미만은 겔 공극 이중 계상이라 설계상 범위 밖) |
| 화학 백엔드 | `stoichiometric`(합성) 또는 `gems3k`(번들 .lst 경로 + 워커 인터프리터 + 상별 겔공극률 맵). 번들 기본값은 `gems_bundles/CNASH/Test-dat.lst`(InverseGems Test 번들 벤더링, C-S-H=CNASH — rev.2에서 InverseGems 정렬을 위해 기본 채택) — `gems_bundle_lst`로 임의 번들(예: `gems_bundles/PC`, CSHQ) 교체 가능. 억제할 클링커 상은 번들이 실제 보유한 것만 요청(수화물 전용 번들 허용) | 필수 |
| 시간 스케줄 | 출력 시각 리스트 + dt 정책(최소 dt, 최대 재시도) | 필수 |

### 1.3 출력

- 시각별 3D 상태: 상별 점유율 배열, 액체/기체 모세관 분율, particle/cluster 라벨.
- 리포트(v2.2): 연결/고립 모세관 공극률 분리, EDT 기반 공극 크기 분포,
  Kozeny-Carman 유효 투수성(리포트 시점 전용, k=C·φ³/(1−φ)², C=평균공극지름²/180).
- 리포트(rev.2, 2-스케일): ① 공극 크기 분포는 부피 가중이며 마스크(0.5) 미만의 부분 충전
  복셀을 슬래브 근사(d=f·h)로 서브복셀 꼬리에 편입 — 모세관 부피를 분포에서 버리지 않는다.
  빈 경계는 절대 µm 고정(0.0625~64, 해상도 무관 — 해상도 간 표 직접 비교 가능; rev.2).
  리포트에 `notes` 필드: summary.json 부재/불량 시 pH 누락을 명시 경고(rev.2).
  ② 전도도 네트워크 유효 상대확산계수 D_eff/D₀ (3축): 셀 전도도 = 모세관 액체분율 +
  겔 보유 수화물 부피 × 0.0025(Garboczi–Bentz C-S-H 상대확산계수, `--gel-rel-diffusivity`
  오버라이드), 면은 조화평균, 배경 플로어 1e-8(보고 하한), Jacobi-CG(결정론, 미수렴은
  status로 정직 보고). 이진 마스크 퍼콜레이션과 달리 서브복셀 액체·겔수 경로가 살아 있어
  후기 재령 수송이 복셀 해상도에서 인위 차단되지 않는다. RT의 D_eff 장과 동일 계산.
  목 보정 노브 `--face-mixing-beta`(0=조화[기본]…1=부분 복셀 면에서 산술, Wiener 유계;
  해상 완료 면은 항상 조화 유지): 해상도 사다리 캘리브레이션 실측 — 1d 사다리는 β=0.6에서
  평탄화(6.0→1.2%)되지만 28d는 35→25%에 그침. 후기 재령의 격자 의존은 굵은 격자의
  6-이웃 그래프에 목 경로 자체가 없는 위상 결손으로, 면 규칙의 한계 밖 — 기본값은 0을
  유지하고 수송 정밀도는 격자 세분(≥64³)과 사다리 불확실성 표기로 담보한다 (실측 기록).
- 시계열 요약(JSON): 상별 alpha, 상별 mol, 물 분배(자유/겔/결합), 총·모세관 공극률, 화학수축 진단,
  accept/reject 카운트, 최대 원장 오차. v3.0/E1: 채널별 고용체 조성 관측치
  `solid_solution_composition` — endmember mol 분해와 Ca/Si·H/Si(규산 겔),
  Al/(Al+Fe)(Al-Fe 고용체) 시계열 (진단 전용, mol 원장이 권위).
  v3.0/E2: 클러스터 해상도 조성 관측치 `cluster_ca_si` — **지배 규산 고용체 채널
  1개**(풀 Si 질량 최대인 다-endmember 채널, report에 `casi_channel` 기록; 전 Si
  고용체 합산은 C-S-H와 하이드로가넷/M-S-H를 섞어 실존하지 않는 조성을 만들므로
  금지 — 리뷰 반영)의 클러스터별 Ca/Si(자기 풀 유래, 더스트 풀 생략)와 report의
  중앙 슬라이스 Ca/Si 지도 PNG(`casi_ckpt_*.png`, 청→적) — SEM-EDS 원소 지도에
  대응하는 공간 조성 산출물.
  v3.0/E3: 리포트 최상위 `binder_oxides` — 레시피 파생 SO3·Na2O·K2O·Na2O당량 wt%
  (밀 성적서 단위, 진단 전용).
- GEMS 백엔드 사용 시: 클러스터별 pH, 이온강도, 상별 원소 조성(parcel).
- Zarr-v2 체크포인트(재시작 가능) + 분석 요약 + PNG 슬라이스 이미지(선택).

### 1.4 명시적 제외 (v2 범위 밖 — 코드도 만들지 않는다)

TINN/서로게이트 학습, 건조/RH 경계·수분 유출, dry-residue 정책,
XCT/SEM 실험 검증 파이프라인, supervoxel/FVM 수송, 민감도 연구 프레임워크,
GUI. 이들을 위한 훅·플래그·빈 인터페이스도 미리 만들지 않는다 (YAGNI).

**v4.0/RT 개정 (2026-08-20): 셀간 수송 제외의 부분 해제.** 방법론 논문 §5.3이 약속한
반응-수송 확장을 범위에 넣는다 — 단, **도메인 그래프 해상도**로만:
- **모드 B — 속도제한 재평형** (`transport.exchange_tau_h`): 스텝당 각 도메인 소유
  고용체의 f=min(1,Δt/τ)만 평형에 제공. τ→0 = 현행 전평형(비트 동일 게이트),
  τ→∞ = 침전 시점 조성 보존. §4.6.1.
- **모드 C — 서브클러스터 평형 도메인** (`transport.domains`): 연결 클러스터를 정적
  타일 교차로 분할한 도메인이 각자 well-mixed 반응기가 되고, 도메인 사이는 공유
  전도 규칙의 그래프 위 암시적 확산(단일 유효 D₀)으로 연결. 타일=격자 = 현행과
  비트 동일(게이트). §4.6.2. 두 모드는 직교·합성 가능하며 config 부재 시 현행 엔진
  그대로(레거시 config_hash 불변).
- **경계 저수조**(RT-W3, `transport.boundary`): 노출면 고정 조성 배스 — 용출(leaching)·
  탄산화 등 노출 문제의 입구. 플럭스는 예약 원장 `boundary_exchanged_elements`로
  부호 계상(§6.1 폐합 항등식 기대변에 이미 포함).
- **여전히 제외**: per-voxel FVM/supervoxel 수송(도메인 그래프가 유일한 수송 해상도),
  건조/RH·수분 유출(용질만 확산, 물 장은 수송 연산자 내 정적), 종별(per-species)
  확산·전기이동 결합(원소 상태에는 화학종 D가 정의되지 않음 — §4.6.2의 공통 D₀
  폐포가 v1 규칙), 하소·온도 구배.

**v2.2 개정 (2026-07-15): 기존 수화물 재용해 제외 해제.** gems3k 백엔드는 클러스터별
**전평형(snapshot)**: 소유 수화물 원소 + 용액 인벤토리 + 방출 원소 + 자유수를 한 시스템으로
평형화하고 결과가 클러스터의 상 집합을 절대 교체한다. CH 소모·상 재배열은 평형에서 발생한다.
StoichiometricBackend는 종전대로 증분(incremental)이다. 입자는 재료별 입자군(§1.2 material_psd)으로
배치된다. 세부 규칙은 §4.5.

**v2.1 개정 (2026-07-15): SCM 제외 해제.** 슬래그/플라이애시/메타카올린/실리카퓸 4종을
kinetic 상으로 승격한다. 조성·밀도·반응도 파라미터는 InverseGems
(`C:\Users\solmo\InverseGems\configs\materials.yaml`, `scm_reaction.yaml`)에서 가져온다:
- SCM 유리는 100 g을 화학식 단위로 하는 원소 조성(산화물 wt% 변환)으로 registry에 등록,
  몰부피 = 100/밀도. 조성 발명 금지 — 측정 산화물 조성 그대로.
- 반응도는 로지스틱 스케줄 α(t) = D + (A−D)/(1+(t/C)^B)^G (시간 일수). 위치·화학 무관여
  (KineticsModel 계약 유지). 클링커 P&K와 함께 BlendedKinetics로 결합.
- SCM 방출 원소는 GemsBackend로만 평형화한다. StoichiometricBackend에는 SCM 규칙이 없으므로
  SCM 질량 > 0 + stoichiometric 조합은 config 검증에서 거부(침묵 폴백 금지).
- 미배정 잔여(inert)는 종전대로 유지된다.

**v3.0/E3 개정 (2026-07-30): 황산염·알칼리 투입 채널 제외 해제.** v2.2까지 바인더는
클링커 4상 + SCM만 받아 OPC 계가 사실상 무황산염이었고(모노설페이트 ~1e-23 mol = 수치
먼지) 알칼리 투입 경로도 없어 공극 용액 pH의 실험 비교가 불가능했다. **가용성 염 담체
5상**을 kinetic 상으로 승격한다: 황산염 `gypsum`/`hemihydrate`/`anhydrite`,
수용성 알칼리 `arcanite`(K2SO4)/`thenardite`(Na2SO4) — `SALT_PHASE_IDS`.
- 몰부피는 **번들 DCH의 V0 실측값**(Gp 74.69, hemihydrate 61.73, Na2SO4 53.33은
  PC·CNASH 동일, K2SO4 65.50은 PC 전용)을 그대로 쓴다 — 용해 후 GEMS 재침전이 같은
  부피 정의를 쓰도록. 무수석고는 두 번들에 없어 결정학값 45.94(M/ρ=136.14/2.963)를
  쓰고 **재침전 경로 없는 용해 전용 담체**로 문서화한다(침묵 대체 금지).
  주의: 번들의 hemihydrate V0 61.73은 ρ=2.35 g/cm³를 뜻해 결정학 반수화물(bassanite,
  2.73)보다 가볍다 — GEMS 부피 정합을 위해 번들값을 택했으므로 반수화물 배합은 광물
  기준보다 약 16% 큰 고체 부피를 차지한다(φ_target·체적 w/c에 영향, 리뷰 반영).
- **용해는 속도식이 아니라 용해도가 정한다(v3.0/E3b, 2026-07-31 사용자 결정).**
  석고는 용액이 불포화면 계속 녹고 포화면 멈추며, AFt가 황을 소비하면 다시 녹는다 —
  적을 수 있는 속도식이 없다. 따라서 엔진은 **속도식을 발명하지 않고**, 매 스텝
  "지금 물이 닿을 수 있는 전량"(§4.2 전도 면수 가중이 0이 아닌 복셀의 담체 부피)을
  평형에 내놓고 **얼마가 고체로 남을지는 GEMS가 결정한다**. 이 상들은 클링커와 달리
  서프레션 대상이 아니므로 평형이 유지하는 몫은 그대로 다시 석출된다. 결과적으로
  담체는 **용해도곱**이 지배하며 맞춘 시간상수가 개입하지 않는다. (E3 초판의 1차
  방출 α(t)=1−exp(−t/τ)와 `SALT_TAU_H_PRESETS`·`kinetics.salt_tau_h`는 근거 없는
  수치였으므로 삭제했다. tabulated 표에 담체 열이 있으면 무시하지 않고 거부한다.)
  단, 담체별 **재침전 가능 여부는 번들에 달렸다**: CNASH 번들에는 K2SO4/arcanite DC가
  없어(수용성 K 화학종만 존재) arcanite는 무수석고와 마찬가지로 **용해 전용**이다 —
  즉 이 번들에서 K는 전량 용액에 남는다(readily soluble alkali의 정확한 거동).
  PC 번들에는 arcanite·syngenite가 있어 재침전 경로가 열린다.
- 접근성이 매 스텝 다시 계산되므로 **t=0에 물에 닿지 않았던 담체 알갱이도 나중에
  드러나면 녹는다**. E3 초판은 α가 첫 스텝에 포화해 그 알갱이가 영구히 unmet으로
  묶였다(w/c 0.25에서 arcanite 10.2% 유실 — 리뷰 실측). 담체는 unmet을 쌓지 않는다:
  못 녹은 담체는 사라진 게 아니라 아직 고체로 그 자리에 있다.
- **as-built 대 레시피**: 소량 담체(0.6 wt% = 32³에서 ~90 복셀)는 입자 샘플러가
  목표를 근사로만 맞춘다. 리포트 `binder_oxides`는 레시피 값과 함께 `*_as_built`
  (초기 `initial_phase_mol` 유래 = RVE가 실제로 담은 양)를 실어 이 격차를 드러낸다 —
  큰 격차는 더 미세한 `material_psd`나 더 큰 격자가 필요하다는 신호이며, 여기서
  조용히 보정하지 않는다 (리뷰 반영).
- 화학수축 분모(`chem_shrinkage_ml_per_g_reacted`)와 §6.3 SCM 비중에서 **염 용해는
  제외**한다 — 염이 녹는 것은 결합재 반응이 아니며, 포함 시 비율이 담체 투입량만큼
  희석되어 §6.3 판정이 뒤집힌다(12h에 "반응 질량"의 22% — 리뷰 실측).
- `phase_volume_fractions`는 무수 담체와 동명의 평형 상을 구분한다(번들이 선언하는
  `hemihydrate`/`arcanite`/`thenardite`가 담체 ID와 충돌 — 재침전물이 미용해 담체를
  덮어써 ~100배 과소보고했음, 리뷰 반영). 중앙 슬라이스 PNG도 담체 전용 색(황토)을
  써서 불활성 필러와 구분한다.
- 담체는 혼합분쇄되지만 별개 고체이므로 **각자 입자군**(SCM과 동일, `material_psd`·
  `material_shape` 키 허용)으로 배치된다 — 클링커 조성에 섞지 않는다.
- StoichiometricBackend는 즉시 침전 모델이라 용액에 남는 염을 표현할 수 없다:
  염 질량 > 0 + stoichiometric 조합은 config 검증에서 거부.
- `KINETIC_PHASE_IDS`/`SOLID_PHASE_IDS`가 길어지므로 **pre-E3 체크포인트는 재시작
  불가**(헤더 채널 목록 비교가 추가 채널을 지목해 하드 실패 — 마이그레이션 코드 없음,
  PRD 0.3). 염 없는 config의 `config_hash`는 불변(salt_tau_h None-pop).
- 진단: `analysis.binder_oxide_diagnostics` — 레시피가 실제로 지닌 SO3·Na2O·K2O·
  Na2O당량 wt%(밀 성적서 단위, 0.658 = M(Na2O)/M(K2O)). 레지스트리 화학식에서 파생하므로
  화학과 어긋날 수 없다.
- **비교성 주의**: 황산염이 실제로 AFt/AFm을 만들면 §6.3 무석고 기준 밴드와 기존
  런의 비교성이 깨진다 — 염을 넣은 런은 별도 검증 매트릭스로 다룬다.

---

## 2. 아키텍처

### 2.1 모듈 구성 (상한 16개)

```text
src/tinn/
  config.py       # Pydantic 설정 스키마 + 단위/범위 검증 + config hash
  registry.py     # 상/성분 레지스트리 (ID, 화학식, 몰질량, 몰부피+basis, 겔공극률)
  geometry.py     # 주기 RVE 초기화: 입자 배치, 3계층 분할, 초기 포화
  kinetics.py     # KineticsModel: TabulatedKinetics + ParrotKilloh(2 프리셋)
  dissolution.py  # 상별 목표 용해량의 사이트 배분 + 소진 사이트 재배분
  transport.py    # 액체 클러스터 라벨링 + overlap-matrix 리매핑
  backend.py      # ReactionBackend 프로토콜 + StoichiometricBackend(합성 다상)
  gems.py         # GemsBackend: 격리 프로세스 xGEMS 워커 + PC 번들 감사/해시
  morphology.py   # 내부/외부 분할 부피 배치, 용량 부족 시 reject
  state.py        # SimulationState: dense 배열 + 테이블 + 원장 (처음부터 phase-vector)
  ledger.py       # 성분/상/물/부피 원장 갱신과 불변식 검사
  engine.py       # 트랜잭션 오케스트레이터: trial → 검사 → commit/rollback, dt 적응
  storage.py      # Zarr-v2 체크포인트 저장/로드/검증/원자적 교체
  analysis.py     # 읽기 전용: 공극률, 연결도(percolation), 상 분율 시계열, 슬라이스 PNG
  cli.py          # run / restart / validate-config / report 서브커맨드
  __init__.py
```

### 2.2 인터페이스 계약 (v1 D1–D12 압축 계승)

- **KineticsModel**: 구간 [t, t+dt]에 대한 상별 목표 alpha 벡터만 반환. 위치·화학·형태 무관여.
  - `TabulatedKinetics`: 단조 보간, 시간 증가·alpha∈[0,1] 검증.
  - `ParrotKilloh`: 상별 min(R_ng, R_df, R_hs) 적분, Blaine/온도/RH/물접근 보정.
    프리셋 `pk_elakneswaran_2018`(기본), `pk_cemgems_2021`. RH<0.55 컷오프.
- **ReactionBackend**: 클러스터별 (기존 용액 인벤토리 + 이번에 방출된 원소)를 받아
  (잔여 용액 인벤토리, 신규 침전 parcel 목록, 상태/잔차)를 반환. 미반응 클링커는 절대 입력하지 않는다.
  좌표를 모른다. 계산 불가 필드는 NaN + `not_available`이지 0이 아니다.
  - `StoichiometricBackend`: 4상 각각에 고정 합성 화학식(C3S→C1.7SH4+1.3CH 등 config 명시)을 적용하는
    결정론 백엔드. GEMS 없이 전체 파이프라인을 구동·테스트하기 위한 것.
  - `GemsBackend`: xGEMS ChemicalEngine, **snapshot 모드(v2.2)**. 입력 = 클러스터 소유 수화물
    원소 + 용액 인벤토리 + 방출 원소 + 자유수; 출력 = 새 상 집합 전체(절대 교체 스냅샷),
    물 소비는 net(재용해 시 음수 허용). 트라이얼당 클러스터당 기본 1회 호출(물 부족·비수렴 시
    방출량만 스케일다운 협상; 소유분은 스케일 불가; 미달분은 unmet 기록), 격리 실행
    (ipmlog.txt/xGEMS.log 오염 방지), 소스 번들 해시 전후 검증, 클링커 상 억제(bound=0),
    cold-start 실패 시 trial reject. 입력은 정준 크기로 스케일 후 출력을 정확히 역스케일
    (평형은 세기성질). 솔버 플로어·O2 시드·검증 슬랙·호출당 폐쇄 잔차(부호)는 주입 원장으로
    정확 계상하고 누적 상한(초기의 1e-6) 초과는 블로킹. 물이 극미한(전체의 1e-3 미만) 비수렴
    클러스터는 해당 스텝 반응을 포기(소유분 동결)하고 unmet 기록. 지속 워커(--serve,
    요청마다 fresh cold-start 엔진 = 스폰 호출과 비트 동일, 번들 감사 유지)는 인프라 토글.
- **MorphologyModel**: parcel의 벌크 외피 부피(V_skel/(1-ε_gel))를 소스 입자 주변
  내부(inner)/외부(outer) 분할로 배치. 부피를 버리거나 임의 이동 금지. 셸 반경(3) 내
  용량 부족 잔여분은 동일 연결 액체 클러스터의 잔여 (공동+액체) 용량에 비례해 용액 통과
  침전(성장 캐스케이드 (c)티어와 동일 물리, 결정론·정확 잔차) — 클러스터 전체 용량
  부족만 reject.
- **TransportModel**: 액체분율 임계 + 면 전도 기준의 6-이웃 주기 클러스터. 화학·배치 무관여.
  (v4.0/RT 확장 — 여전히 화학 무관여): ① `partition_domains(liquid, labels, n_clusters,
  cfg)` — 결정론·주기 인지 도메인 분할, 도메인은 클러스터의 세분(refinement)이어야 하며
  타일=격자면 라벨을 그대로 반환(현행 경로의 리터럴 항등). ② `conductance_field(...)` —
  analysis의 D_eff 네트워크와 **동일한** 셀 전도 규칙(액체 + 0.0025×겔 수화물)의 공용
  헬퍼(§1.3 약속 이행; NETWORK_FLOOR는 리포트 전용으로 analysis에 남는다 — 물리에
  넣으면 봉인 고체를 1e-8로 새게 한다). ③ `exchange_be(graph, inventory, water, dt,
  d0, reservoir)` — 도메인 그래프 위 암시적(backward Euler) 확산을 **엣지 플럭스
  형태**로 적용(같은 float를 한쪽에서 빼고 다른 쪽에 더함 = 원소 보존이 구성상 정확).
  `label_clusters`는 `periodic_axes` 옵션을 얻는다(전주기 기본 경로는 바이트 동일).
- **ReactionBackend 급이 계약(v4.0/RT)**: 모드 B에서 백엔드에 먹이는 소유분은
  offered = f ⊙ owned로 축소되고, 원장 차감도 정확히 offered만큼이다("빼는 수치 =
  먹인 수치" 원칙 유지). 유보분 (1−f)은 그 스텝 화학 동결(불포화에도 불용 — 교체
  회전율이 τ가 정하는 물리). 신규 안정 거부 식별자: `transport_failure`(BE 미수렴),
  `balance_domain:*`/`balance_exchange`(§6.1).
- **Engine**: 유일한 상태 변경 주체. trial 생성 → kinetics → dissolution → backend → morphology →
  ledger/불변식 → commit 또는 rollback+dt 축소. 거부 사유는 안정 식별자
  (`placement_capacity`, `insufficient_water`, `cluster_dryout`, `backend_failure`, `balance_*`).

### 2.3 상태 모델 (v1 state_vector 압축, 처음부터 다상)

**Dense (z,y,x 순, float64):**
`anhydrous_fraction[phase]`, `hydrate_fraction[phase]`(벌크 외피), `capillary_liquid`,
`capillary_gas`(고정 RVE 잔여), `particle_id`(int64, -1=없음), `cluster_id`(int64, 스냅샷-로컬).
복셀 항등식: Σ고체 + Σ수화물 + 액체 + 기체 = 1 (허용오차 내, 클리핑 금지).

**테이블:** particles(3계층 공통), subgrid_bins, clusters(+원소 인벤토리 벡터),
product_parcels(시각·클러스터·상별 평형 이벤트 행 — v2.2: 보유량 권위는 hydrate_* 원장 벡터이며
파슬 행은 이벤트 기록; 수화물 원소 원장은 채널별 (H,E) `hydrate_elements_ch`, 원장 갱신은 부호 있음),
remap_events(overlap 행렬 기록).

**Endmember 원장 (E1, v3.0)**: 조성은 원장에, 공간은 dense에 — dense는 채널당
부피분율 1개 그대로(메모리 증가 0), 원장에 런 스코프 `endmember_ids`
((상, DC) 쌍, 백엔드 선언 — gems3k는 번들 info의 phase_species, 합성은 채널=endmember)와
`endmember_mol (M,)`(부호 있는 현재 보유), `endmember_elements (M,E)`(번들 DCH의
A행렬 행 그대로 — 하드코딩·고정 화학식 fallback 금지)를 둔다. 워커는 elements 응답에
상별 `phase_species_mol`을 싣고(기존 endmember 합 검증과 같은 수에서 나옴), 파슬이
`endmember_mol`을 운반(정준 스케일 1/s 동일 적용; None=단일 endmember 상), 엔진은
소유권 분배와 동일한 부피 비례로 클러스터별 owned endmember를 계산해
"원장에서 빼는 수치 = 백엔드에 먹인 수치" 원칙을 endmember까지 확장한다.
`boundary_exchanged_elements (E,)`는 RT 경계 저수조용 예약 필드(현재 0,
폐합 기대값에 이미 포함)로 FORMAT_VERSION 3 개편에 동승했다 — v4.0/RT 마일스톤
번호로는 **RT-W3**에서 활성화된다(E1 당시 표기 "RT-W2"는 구 번호).

**도메인 키 상태 (v4.0/RT)**: 모드 C에서 반응기 키는 클러스터가 아니라 **도메인**이다.
`cluster_inventory (D,E)`·`cluster_endmember_mol (D,M)`은 이름 그대로 두되 행이
도메인에 키잉된다(자명 분할 = 클러스터 ⇒ v3와 내용 동일). 분할 자체는 저장하지
않는다 — 저장된 액체장의 순수 결정론 함수로 스텝 시작·재시작 시 재계산(재계산 =
저장과 동일함을 테스트로 고정). 행수 계약("행수 = 도메인 수 또는 0, 그 외 손상")과
액체 겹침 리매핑은 클러스터 계약을 그대로 일반화한다. 모드 B는 **새 배열이 없다** —
기존 풀의 갱신 규칙이 절대 교체에서 블렌드(f=1 경로는 현행 코드 경로 그대로)로
바뀔 뿐이다. **FORMAT_VERSION 5는 RT-P0b에서 1회 단절**(2026-09-02): 동결
스페시에이션 `domain_species_mol (D,S)` + 헤더 `aq_species_ids` + **Tier 1
예약** `domain_sorbed_mol (0,E)`(RT-S1a까지 제로행 — v4의 boundary_water_mol
예약 전례: 단절 1회로 앵커 재고정 1회). 그 이전, **FORMAT_VERSION 4는
RT-W2에서 1회 단절**: 도메인 키 행 의미 +
GEM 경제 스냅샷 배열(`domain_eq_*` — 재시작 비트 동일성에 필수) + 예약 스칼라
`boundary_water_mol`(0; RT-W3 물 교환 대비 동승 — 재단절 없음) + 모드 C 런의
parcels 채널 집계 행. RT-W1(모드 B)은 스키마 무변경 — v3 유지. 마이그레이션 코드
없음(0장 원칙 3); v3 체크포인트는 자기진단 메시지로 거부.

**클러스터별 endmember 풀 (E2, v3.0)**: `cluster_endmember_mol (K,M)` —
클러스터별 조성 기억. cluster_inventory와 동일한 라벨링 계약(행수 = 라벨 수 또는
0 = 미보유; 그 외는 손상으로 거부)·동일한 액체 겹침 리매핑을 따른다. 급이량은
기존 부피 비례 그대로이고, 조성은 **covered-portion 규칙**(E2 리뷰 반영):
풀은 자기가 실제로 기억하는 질량 covered = min(풀 합, 급이량)에 대해서만 권위이고,
잔여(재습윤·건조 지역 귀속분, 풀 부재 클러스터 전체)는 글로벌 채널 비율로 급이한다.
두 비율원 모두 음수 더스트를 클립해 심플렉스 위에 둔다(혼합 부호 더스트를 상쇄된
합으로 나누면 1/eps까지 증폭 — 리뷰 발견). 이로써 endmember별 차감이 풀 내용
+ 보유 비례 항으로 유계가 되어, 작은 풀이 큰 급이량의 조성을 발명해 글로벌
원장을 음수로 몰 수 없다(무계 비율 급이의 침묵 손상 경로 — 리뷰 발견; §6.1
1b(c) 비음수 검사가 최종 증인). 해결(solved)된 클러스터의 풀은 그 스텝 자신의
파슬로 **절대 교체**(누적 아님 — 자기치유), 동결 클러스터는 유지, 겹침 0 행은
소멸(보존은 글로벌 원장이 지므로 dryout 아님). 외부 v3 체크포인트가 없어
FORMAT_VERSION 3에 동승(재단절 없음).

**헤더:** `kinetic_phase_ids`(순서 고정), `hydrate_phase_ids`(런 스코프: 합성=레지스트리 4상,
gems3k=번들 고체상 목록), `phase_alpha`/`initial_phase_mol`/`unmet_mol` 벡터,
time, dt, config_hash(워커 인터프리터 경로 제외), code_version(git), backend_id,
RNG 전체 상태, accept/reject 카운트, 수화물 원소/외피부피/주입 원장 벡터.

**물 원장:** 자유 모세관수 / 겔수(수화물 외피 내부, 점유율 비가산) / 결합수(상 조성 유래) — 상호배타,
mol이 권위이고 kg은 파생.

**체크포인트:** Zarr-v2 디렉터리 스토어, 무압축 raw chunk + JSON 테이블, manifest 체크섬,
임시 디렉터리 작성→검증→원자적 rename. 재시작은 무중단 실행과 비트단위 동일해야 한다.

---

## 3. 마일스톤 (각각 독립 세션 1~2회 분량, 순서 고정)

각 마일스톤의 **완료 조건(DoD)** = ① 명시된 스모크 런 성공 ② 명시된 최소 테스트 통과 ③ git 커밋.
그 이상의 테스트·문서·검증을 추가하지 않는다.

### M0 — 골격과 초기화
- 패키지 골격, `config.py`, `registry.py`, `geometry.py`, `cli.py`의 `validate-config`.
- 32³ 합성 PSD로 다상(C3S 단독 배합도 4상 스키마의 특수형) RVE 초기화.
- DoD: 목표 고체분율·w/c 달성(오차 보고), 동일 시드 → 동일 배열 해시. 테스트 ~15개.

### M1 — 보존 코어 (합성 백엔드 전체 루프)
- `state/ledger/dissolution/transport/backend(합성)/morphology/engine/storage` 전체 연결.
- 32³ C3S-물 런: alpha 0→0.35, 롤백 포함, 체크포인트/재시작 비트단위 동등.
- DoD: §6.1 수치 보존 전 항목 통과. 테스트 ~50개 (v1의 G/K/L/A/C/P 매트릭스에서 핵심만 발췌).

### M2 — P&K 동역학과 4상 구동
- `kinetics.py`에 ParrotKilloh 추가, NIST SRM 114q 레시피(60/14/7/10/9, Blaine 381.8 m²/kg)로
  4상 alpha 스케줄 생성, 합성 백엔드로 3D 런.
- DoD: 두 프리셋이 §6.2의 P&K 앵커 값 재현(단위 테스트), 4상 3D 런에서 상별 원장 폐쇄. 테스트 ~20개.

### M3 — GEMS 0D 프로브
- `gems.py`: PC 번들 로드, 격리 워커, 해시 감사. 0D 누적 화학 프로브
  (P&K가 방출한 양만 평형화, 클링커 억제).
- DoD: §6.2 GEMS 앵커 2건 재현(회귀 스냅샷), C3S 0D 프로브에서 원소·물 수지 폐쇄. 테스트 ~15개.

### M4 — GEMS→3D 결합
- GemsBackend를 engine에 연결: 클러스터별 방출 원소 집계 → xGEMS → parcel 커밋 →
  형태 배치. CSHQ는 endmember 합으로 검증하고 고정 화학식 재해석 금지.
  기존 수화물 재용해는 비활성(백엔드 입력에서 제외).
- DoD: 32³ OPC 런 1/3/7일 시점 완주(또는 명시적 feasibility ceiling 보고),
  §6.1 폐쇄 + §6.3 sanity band 리포트 생성. 테스트 ~25개.

### M5 — 분석과 리포트
- `analysis.py`: 총/모세관 공극률 시계열, 액체 percolation 여부, 상 분율, 중앙 슬라이스 PNG,
  `cli report` 서브커맨드로 요약 JSON+이미지 일괄 생성.
- DoD: M4 런 산출물에서 리포트 생성, §6.3 sanity band 자동 판정 포함. 테스트 ~15개.

**마일스톤 밖 작업 금지 목록**: M1 전에 GEMS 코드 작성 금지, M4 전에 analysis 작성 금지,
어떤 시점에도 §1.4 제외 항목 착수 금지.

### v4.0/RT 마일스톤 (2026-08-20, rt-dev 브랜치; DoD 형식 동일 — 스모크 + 최소 테스트 + 커밋)

### RT-W0 — 부트스트랩
- f7be88b clone 검증, rt-dev 분기, 플랫폼 WIP에서 aqueous 진단(backend.py+gems.py)
  **verbatim 포트**(상류 합류 시 텍스트 동일 → 병합 자명), 본 PRD 개정, 기준 앵커 런
  2건(§6.2) 실측 기록.
- DoD: GEMS 유/무 스위트 그린(225 통과 기준), T1(aqueous 진단) 통과, 앵커 해시 기록.

### RT-W1 — 모드 B (속도제한 재평형; 포맷 무단절)
- `transport.exchange_tau_h`(+상별 오버라이드) config 축, 엔진 offered = f⊙owned 급이,
  풀 블렌드 갱신(f=1은 현행 코드 경로 그대로).
- DoD: ① τ≤dt 이중 런 full_hash 동일 ② 0<f<1 GEMS 런 §6.1 전 폐합(1b 포함)
  ③ 모드 B 재시작 비트 동일 ④ 모드 부재 재런이 RT-W0 앵커 재현. 테스트 ~6개.

### RT-W2 — 모드 C 봉인계 (도메인 분할 + 그래프 확산; FORMAT_VERSION 4)
- `transport.py`: conductance_field 공용화 → label_domains/build_domain_graph →
  exchange_be(BE 플럭스 형태). 엔진: 도메인 루프 통일(현행 = 자명 분할 경로),
  T→R 분할 삽입, 순차 용량 청구 공간충전 게이트, 클러스터 수위 물 정산,
  trace-water 재스코프, fold 드라이아웃, GEM 경제(더티 플래그·연령 상한·호출 예산).
- DoD: ① 1-도메인 극한 비트 동일(합성 **및** GEMS) ② 해석해 3종(§6.2 앵커) ③ 플럭스
  반대칭 폐합 ④ 결정론 재현 ⑤ 경제 상태 포함 재시작 비트 동일 ⑥ 스텝 시간 비율
  기록 ⑦ v3 체크포인트 자기진단 거부. 테스트 ~10개.

### RT-W3 — 경계 저수조 (설계 리뷰 2026-08-20 완료 — §4.6.3 확정판 참조)
- `transport.boundary`: 노출축 비주기 라벨링·그래프(`periodic_axes`), 고스트
  RHS 소거 결합(`boundary_coupling` + `exchange_be(bath=…)`),
  `boundary_exchanged_elements` 부호 계상(용질 전용 — `boundary_water_mol`은 0 유지).
- DoD (설계 리뷰 개정 — 종전 ① "선언-무플럭스 = 봉인 비트 동일"은 성립 불가:
  결합항이 행렬·우변을 바꾼다):
  ① **구조 부재 게이트**: `boundary` 부재 config = 레거시 경로 바이트 동일
     (기존 앵커·스위트가 담보) + 전주기 `periodic_axes` 기본 경로 바이트 동일
     + 결합 전량 0(g_bnd=0) 배스 = bath 부재와 비트 동일(퇴화 게이트).
  ② **해석 앵커 4**: 1-도메인+배스 BE 1스텝 c⁺−c_R = (c−c_R)/(1+Δt·λ_R),
     λ_R = D₀·G_AR/(p·W) — rel 1e-12; c_res = c(동일 float)이면 |플럭스| ≤
     CG 공차 유계(비트 0 아님).
  ③ **양부호 폐합**: 순수수 유출(음) / NaOH 유입(양) 모두 §6.1 원소 폐합 +
     `balance_exchange` 통과 — 원장 변경 0으로.
  ④ **거울 대칭**: z-거울 픽스처 + 양면 동일 배스 ⇒ 층별 교환 델타 거울 대칭
     (rel 1e-12; 합산 순서상 비트 아님).
  ⑤ **재시작 비트 동일**: 활성 경계 런 중단/재개 = 무중단(누적 경계 원장 포함).
  테스트 ~9개.

### RT 리뷰 게이트 2차 (2026-08-20, /code-review W2b–W4b 범위) — correctness 반영 완료

수정 11건: ① 밀봉→노출 전환 리매핑을 **무조건** 수행(개수 프록시는 seam 분리가
도메인 수를 보존하며 id만 순열시키는 경우 — 3개 각도가 실행 재현 — 를 놓친다;
동일 액체 리매핑은 순열에 비트 정확) ② start_h를 일치 출력 시각으로 스냅(검증
1e-9 vs 활성화 1e-12 ε 불일치) ③ `_neighbor_best_label`·링 릴레이 노출축
비주기화(건조 면 사이트가 랩 너머 반대면 반응기로 귀속되는 재현 확인 — 귀속은
원장 질량을 옮긴다) ④ 플러시-재시도에 배수 시그니처 가드(Σ|inv|/water <
1e-4 mol/mol — 워커 타임아웃 등 건강한 용액의 오유출 차단) ⑤ 초과인출 가드:
유예 도메인 예산 청구 제외 + mode B 북키핑(pool_fed/withheld) 동기 축소 +
위반 채널 한정·errstate 호이스트 ⑥ 전건조 격자 recon_cl IndexError 가드
⑦ casi 지도·Ca/Si 관측치의 도메인/클러스터 행공간 조인 수정(`analysis.
reactor_labels` — 분할 재계산) ⑧ pH sanity band 분모를 부모 클러스터 점유율로
(미세 분할에서 밴드 무음 비활성화) ⑨ 리포트에 PRD 의무 warn 2종 구현
(`boundary_front_breakthrough`, supply>0.7 노트) ⑩ 용출 드라이버 재실행 크래시
(디렉토리 정리)·registry_for ⑪ 배스 연결 정의 단일화. 기록만: dt 윈도 스냅
성장 규칙은 상류 verbatim 유지(캡 경계 스래싱 관찰 — 병합 시 재론), 단순화
소항목 일부 반영.

**W5 효율 백로그(리뷰 실측, 128³·K≈4×10³ 목표 스케일 기준 — 현행 32³ 실측에는
비발현)**: ① 모드 C remap_events 무제한 성장(스텝당 ~D행)이 clone() 딥카피와
결합해 런 후반 O(D·S²) — 비항등 이벤트만 기록 또는 배열화 ② 스냅샷 배치
루프의 도메인당 전격자 스캔(bincount 사전집계로 대체 가능, 실측 7.7→3.4 ms)
③ exchange_be matvec의 add.at → 열별 bincount(실측 3.5×) ④ 초과인출 가드
발화 시 1.16 s@D=4096(채널 한정으로 1차 완화됨, 잔여는 벡터화) ⑤
remap_inventories 쌍별 파이썬 루프 벡터화.

### RT-W4 실측 기록 (2026-08-20, 1차 정성 통과 — 기구 검증 완료, 정량 √t는 W4.1)

Deschner OPC 32³, 밴드 타일 (2,32,32), 168 h 밀봉 수화(dt 2–6 h) → z-low
폭기 순수수(O 0.5 mol/m³) 노출, 1.2 h 용출(dt 5.04 s, ~860 스텝), D₀ 사다리
0.8/2.0/5.3×10⁻⁹ m²/s. 세 케이스 모두 **엔드-투-엔드 완주, 전 게이트 그린**
(벽시계 2732/2869/3705 s). CH 전선: 0→1~2(0.45–0.6 h)→4 복셀(1.0–1.2 h),
시간 단조·√t 양상. **핵심 소견: 전선이 D₀에 무감(3케이스 동일)하고
`boundary_supply_ratio` = 0.89/0.95/0.97** — §4.6.3 dt 정책이 예측한 공급 제한
국면의 협공 증거(dt 5 s ≈ dt* 경계). 겉보기 a ≈ 20 µm/√day는 공급 상한이 만든
하한이며 문헌(100–200, 미성숙 ~2×)과의 정량 비교는 **W4.1 = dt 사다리(2 s/1 s,
Γ 분석상 관통당 1000–2000+ 스텝)**로 수행한다 — supply_ratio가 0.5 아래로
내려가는 dt에서 a를 재피팅. 진단 지표 설계(공급비 + D₀ 무감도)가 국면을
자가-판별함을 실증한 것이 이번 1차의 성과.

### RT-W4.1 실측 기록 (2026-08-21, dt 사다리 완료 — 공급 상한 해소, 32³ 한계 확정)

dt 프로브(0.12 h 창, D₀ 2e-9): supply 5.04 s→0.95 / 2 s→0.89 / 1 s→0.82 /
**0.5 s→0.71** — dt* ∝ x_f라 초기 국면은 어떤 실용 dt에서도 부분 공급 제한.
전체 창 사다리는 dt=0.5 s(8,640 스텝, Γ 기준 충족)로 실행, 두 신규 결함 수정
후 3케이스 완주(§4.6.3 고아 dryout 인계 — 발화 정확 1회 2.744e-20 mol;
배스 앵커 경계 기장 — 리부킹 ~9e-16 mol/0.4 h):

- **공급 상한 해소**: supply mean 0.34/0.42/0.39 (max 0.84/0.90/0.78) —
  1차의 0.89~0.97에서 목표 <0.5 달성.
- **실속도의 결론 = 32³은 전선을 담을 수 없다**: √(D_e·t)가 ~6분에 RVE
  32 µm를 초과 → 심부까지 불포화 즉시 침투, CH는 예리한 전선 없이 준균일
  고갈(t=0.1 h에 전 깊이 60~90% 소모). D₀ 2.0/5.3e-9는 0.15~0.2 h에 CH=0.
  1차의 "전선 0→4 vox/1.2 h, a≈20"은 공급 상한 인공물이었음이 확정.
- **D₀ 민감도 복원**: CH 반감기 t_half = 0.109/0.072/0.059 h (0.8/2.0/5.3e-9)
  → **t_half ∝ D₀^−0.33** (공급 제한 0 ↔ 확산 지배 0.5 사이; 출력 격자
  0.05 h의 보간 분해능 한계와 초기 알칼리 세척·동결 간섭이 잔여 편차).
- **문헌 정합(관통 시간)**: 미성숙 a ≈ 200–400 µm/√day의 32 µm 관통
  0.15~0.6 h ↔ 실측 완전 고갈 0.15~0.3 h. Ca 누적 유출 1.11/1.27/1.56e-10
  mol(초기 Ca의 24~34%).
- **저 D₀의 동결 잠금(실측)**: 0.8e-9에서 CH 16.7% 잔류 고정 — nonconv
  17,926 + 물 32,442 도메인·스텝(스텝당 ~7/30 도메인) 동결 개체군에 갇힘.
  Ca 유출은 C-S-H 탈칼슘으로 지속(1.02→1.11e-10). 느린 재공급 → 배수 전선
  도메인 동결 → 잔류 CH 용출 제외의 인과가 지표로 자가-보고됨.
- **√t 전선(문헌 a 직접 피팅) 검증은 64³+ 필요** — sharp-front 조건
  √(D_e·t) ≪ L이 32³에서 원리적으로 불성립. W4.2 후보로 이관: 64³(64 µm)는
  전선 창 4배·dt* 2배 완화(벽시계 ~10–20 h/케이스 추정), 정공법은
  128³(128 µm).

### RT-W4.2 실측 기록 (2026-08-23, 64³ 전선 — 문헌 a 직접 대조 성립)

Deschner OPC **64³ @ 1 µm(64 µm)**, 밴드 타일 (2,64,64), 168 h 밀봉 → z-low
폭기수 노출 **0.6 h 용출, dt=1 s(2,160 스텝)**, D₀ 0.8/2.0/5.3e-9. 3케이스
완주(벽시계 165k/151k/181k s ≈ 42–50 h). 세척 지연 t₀ = 0.09 h(전선 0인 마지막
출력)를 보정한 x_f = a√(t−t₀) 피팅 — **주의: 이 t₀는 순수 물리가 아니라 dt
의존 양이다**(W4.2b에서 dt=0.4 s면 t₀ = 0.02 h로 줄어듦 — 공급 상한이 초기
전선 출현을 지연시킨 몫이 섞여 있다):

| D₀ (m²/s) | supply mean | a_raw | **a (t₀ 보정)** | √D₀ 예측 | 비 |
|---|---|---|---|---|---|
| 0.8e-9 | **0.449 (청정)** | 213 | **251** | (앵커) | 1.00 |
| 2.0e-9 | 0.687 | 288 | 336 | 397 | 0.85 |
| 5.3e-9 | 0.825 | 251 | 296 | 646 | 0.46 |

- **문헌 대조 성립(W4의 최종 목표)**: a = 251–336 µm/√day는 성숙 페이스트
  100–200의 **미성숙 ~2× 대역(200–400)**에 정확히 든다. 7일 페이스트를 용출한
  이번 프로토콜에 대해 물리적으로 옳은 값이며, 32³에서 불가능했던 √t 전선의
  **직접** 피팅이 64³에서 처음 성립했다.
- **√D₀ 결손이 공급 지표로 완전히 설명됨**: a/a_√D₀ = 1.00/0.85/0.46이
  supply mean 0.449/0.687/0.825와 단조 대응. dt* ∝ x_f/D_e이므로 **고정 dt에서
  D₀를 올리면 공급 제한에 재진입**한다 — 스케일링 시험은 dt ∝ 1/D₀로 각 케이스를
  청정화해야 성립한다 — **W4.2b에서 인과 확정(아래)**. 겉보기 지수
  a ∝ D₀^+0.09는 이 혼합 국면의 산물이지 물리가 아니다.
- **전선은 여전히 확산형(sharp 아님)**: 10–90% 폭 = 19/30/24 복셀, 최종 원벽
  CH 잔량 57%/34%/0% — √(D_e·t)가 관측 창 후반에 64 µm를 넘어 원벽까지 소모가
  닿는다. 즉 **64³은 "전선 속도 측정에는 충분, 전선 형상 분해에는 부족"**.
  형상·후반 √t 선형성까지 보려면 **128³**(창 4배, dt* 2배)이 정공법.
- 저 D₀ 케이스가 가장 신뢰도 높은 단일 측정(공급 청정 + 전 6점 단조 전진,
  정체 없음). 고 D₀ 두 케이스는 0.35 h 이후 전선 정체 — 원인은 동결이 아니라
  **RVE 고갈**(모든 층이 half-peak 아래 → 전선 지표의 기준 자체가 소멸).

### RT-W4.2b 실측 기록 (2026-08-26, 공급-청정 확인 — √t 법칙·√D₀ 인과 확정)

동일 64³·D₀ = 2.0e-9에서 **dt만 1 s → 0.4 s**로 낮추고 창을 0.27 h로 잘라
재측정(2,430 스텝; 중단 후 ckpt_005에서 재개, 재개분 1,170 스텝의 supply
평균 0.534 / 최대 0.750 — dt=1 s의 0.687에서 하락). 결과:

| 비교(동일 창 ≤0.27 h) | a (µm/√day) | 점별 a | 산포(max/min) |
|---|---|---|---|
| dt = 1.0 s, t₀ = 0.02 | 311 | 170, 346, 353 | 2.08 |
| dt = 0.4 s, t₀ = 0.02 | 365 | 226, 370, 396, 381, 353 | 1.75 |
| **dt = 0.4 s, 확립 구간(t ≥ 0.09 h)** | **371** | 370, 396, 381, 353 | **1.12** |

- **√t 법칙의 직접 입증**: 미세 dt의 확립 전선은 네 개 독립 출력에서 점별 a가
  **±6% 안에 일정**(370/396/381/353). 이는 피팅 산물이 아니라 x_f ∝ √t가
  실제로 성립한다는 뜻이다. 조대 dt에서는 같은 창의 점별 산포가 2.08로
  벌어지고 첫 점이 170으로 이탈 — §4.6.3이 예고한 "공급 제한 = 지연 후
  따라잡기" 궤적 그대로다.
- **√D₀ 인과 확정**: 공급-청정 앵커(0.8e-9, a = 251)의 √D₀ 예측 397 대비
  dt=1 s는 311(0.78), dt=0.4 s 확립 구간은 **371(0.94)** — dt만 절반으로
  줄였을 때 a가 예측선으로 이동했다. 즉 W4.2의 √D₀ 결손은 물리가 아니라
  공급 제한이며, 지표(supply mean)가 그 크기를 정량 예보했다.
- **누적량은 dt에 강건**: 두 dt 모두 t = 0.27 h에서 x_f = 36 복셀로 정확히
  일치한다 — 왜곡되는 것은 초기 궤적(전선 출현 시점)이지 총 용출량이 아니다.
- 운영 함의: **전선 궤적을 정량화하려면 supply mean ≲ 0.5가 되도록 dt를
  D₀에 반비례로 잡아야 한다**(§4.6.3 dt 정책의 실측 확증). dt=0.4 s에서도
  0.534로 경계에 있어 5.3e-9급은 dt ≈ 0.15 s가 필요하다(미실행).

### RT-W4 — 검증 응용 (미정 — W2 결과 후 선택)
- 후보 기록(외부 검토 2026-08-20 우선순위 반영): **② Ca 용출(순수수)
  프로파일 — 첫 응용 권고**(√t 전선 문헌 앵커 최다; 순수수에서는 단일 D₀
  폐포의 NP 대비 오차 무시 수준; D₀ 민감도 0.8~5.3e-9 1회 실측·표기)
  ① Deschner FA 블렌드 조성 zoning 재현(방법론 논문 연속;
  `scm_logistic_override` verbatim 포트 필요; 타일 해상도 관건 = W2b 의존)
  ③ 탄산화 전선(용존 탄산 한정; 수송 전 gas 분리 필요 — §4.6.2 주의;
  클로깅 국면의 분할 오차·전도장 1스텝 지연은 착수 시 dt 정책과 재점검).
- DoD: 응용별 문헌 sanity(§6.3 스타일 info/warn, 비블로킹). 테스트 ~1개.

**RT 밖 작업 금지**: W2 전에 boundary 코드 금지, W4 전에 응용 config 금지,
플랫폼 상류 저장소(`TINN platform v2`)의 작업트리·runs/ 접촉 금지(fetch만 허용).

---

## 4. 핵심 알고리즘 명세 (구현 시 그대로 따를 것)

### 4.1 P&K 상별 속도 (day⁻¹)

R_ng = (K1/N1)(1−α)[−ln(1−α)]^(1−N1),
R_df = K2(1−α)^(2/3) / [1−(1−α)^(1/3)],
R_hs = K3(1−α)^N3, 제어속도 = min(세 후보).
보정: Blaine 비 × Arrhenius(프리셋별 기준온도 293.15/298.15 K) × RH 인자 × 물접근 인자.
프리셋별 차이: 2018은 전체 제어속도에 표면 스케일링, 2021은 nucleation/growth에만.
총 클링커 alpha = 4상 질량가중 alpha / 4상 분율 합 (전체 시멘트 질량 아님).
alpha 시드·적분 스텝은 config 명시 수치 정책.
**가용성 염 담체(v3.0/E3b)**: 동역학 모듈은 이들에 대해 **아무 속도식도 갖지 않는다** —
alpha 슬롯은 항상 0이고, 방출량은 엔진이 접근성에서 직접 정한다(§1.2/§4.2). 맞춘
시간상수는 발명된 수치이므로 두지 않는다.

### 4.2 용해 배분

상별 목표 Δn = 초기 mol × Δalpha. 물 접근 가능 사이트에 전도 면수 가중으로 배분:
액체 보유 면은 1, 수화물 보유 면은 GEL_FACE_WEIGHT(0.28, 대표 겔공극률) —
겔공극수가 코팅 통과 이온 통로이며, P&K 확산 단계가 껍질 저항을 이미 경험적으로
인코딩하므로 액체 접촉만의 기하 게이트는 이중 계상이었다 (v2.2 rev.2, 2026-07-16).
액체·수화물 어느 면도 없는 입자 내부 복셀은 여전히 접근 불가. 코팅 사이트의 클러스터
귀속은 라벨을 링 단위로 결정론 전파(고체 통과 BFS)해 최근접 클러스터로. 소진 사이트
잔여는 반복 재배분. `phase_id` 필터 필수. 가중치는 상대일 뿐 절대 속도가 아니다.
미달성분은 `unmet_mol`로 기록 (은폐 금지). per-voxel 용질 수송(FVM)은 §1.4 제외 유지 —
본 규칙은 "클러스터 내 즉시 혼합" 폐포 가정이며, v4.0/RT 모드 C가 그 자리를
**도메인 내 즉시 혼합 + 도메인 간 확산**(§4.6.2)으로 대체한다(도메인=클러스터면 현행과 동일).

### 4.3 클러스터 리매핑

이전/신규 액체장의 물리적 겹침부피 Ω_ab로 성분·잔여수를 비례 배분(merge/split 동시 지원).
인벤토리 보유 클러스터의 겹침 0 = `cluster_dryout` reject.

### 4.4 부피 규약

수화물 벌크 외피 = 골격/(1−ε_gel), basis(`solid_skeleton`|`bulk_envelope`)는 레지스트리 필수 필드,
누락은 하드 에러. 겔수는 질량 원장에만 계상. capillary_gas는 고정 RVE 잔여이고
화학수축은 동일 원장의 진단값(이중 계상 금지).

### 4.5 전평형 재용해 규칙 (v2.2)

- **소유권(Scheme S)**: 매 스텝 recon 파티션(자체 액체 라벨, 없으면 최다 접촉 이웃 클러스터)이
  각 수화물 채널의 dense 부피를 분할하고, 채널별 원소/mol 풀을 부피 비례로 클러스터에 배분한다.
  건조 지역(recon=-1) 잔여분은 그 스텝 화학 동결. 원장에서 빼는 수치 = 백엔드에 먹인 수치이므로
  분배 float 오차와 무관하게 폐쇄가 정확하다. **조성(E2, v3.0)**: 급이 조성은 클러스터
  자신의 endmember 풀에서 covered-portion 규칙(§2.3 — 풀이 기억하는 질량까지만 풀
  권위, 잔여는 글로벌 비율)으로 오고, 급이 원소는 그 endmember × DCH 원소행으로
  유도되어 endmember 폐합 1b(b)의 급이변이 구성상 정확해진다. 글로벌 평균 조성은
  풀 미커버 질량의 fallback으로만 남는다 — E1의 문서화된 근사가 "풀이 기억하는
  질량에서는 제거"로 승격.
- **제거 우선**: 스냅샷 diff의 음수 채널을 먼저 비례+결정론적 잔차 보정으로 제거(요청=제거 정확),
  해제 부피는 배치 용량으로 선공급 후 잔여는 액체. 성장 앵커는 해제/공동 → 기존 수화물 표면 →
  공극 액체 순 캐스케이드(동일 클러스터 한정). 배치 수지는 성장·제거 양쪽 gross로 검사.
- **물**: net 소비(음수 = 결합수 복귀)는 클러스터 자체 기체 공간에서 액체로 복귀시키며,
  기체가 부족하면 balance_water 거부. 자유+겔+결합 분배 항등식은 부호 있는 이동으로 유지.
- **공간 충전 한계**: 평형 성장 수요가 클러스터 전체 공극 용량(액체 + 그 스텝 공동 +
  자체 재용해 해제분)을 초과하면 — 스냅샷 델타는 dt에 비례하지 않으므로 거부는 곧 런 중단 —
  해당 클러스터를 trace-water 포켓과 동일하게 그 스텝 동결한다(용출은 unmet으로 반환,
  소유 상·인벤토리 유지). 가득 찬 포켓의 국소 수화 정지는 물리적 거동이다.
  rev.2 리뷰 반영: 동결 판정에 1e-9 상대 마진(근사-정확 적합의 ulp 불일치 방지),
  오버플로 배치에 remove()와 동일한 1e-12 더스트 공차 + 음수 더스트 용량 제외.
  코팅 사이트의 링 전파는 겔 보유 복셀·사이트로만 중계(기체·맨 입자 내부 관통 금지),
  어떤 클러스터에도 도달 불가한 사이트는 거부가 아니라 unmet 반환(전체 건조 시에도
  우아한 저하). **물 재배치 한계(2026-07-17 재현·해소)**: 무기체 포화 포켓에서 성장
  변위수가 화학·겔 소비를 초과하면 balance_water가 모든 dt에서 거부됨을 실증 —
  동결 판정에 변위수 조건(성장 − 공동 − 소비수 부피 > 클러스터 기체 공간이면 동결)을
  추가해 해소. 물을 밀어낼 곳 없는 포켓의 수화 정지는 공간 충전과 동일한 물리다.

### 4.6 반응-수송 명세 (v4.0/RT — 구현 시 그대로 따를 것)

#### 4.6.1 모드 B — 속도제한 재평형 (`transport.exchange_tau_h`)

- 채널별 제공분율 **f_h = 1 (τ_eff ≤ 0) / min(1, Δt_시도 / τ_eff)**. τ_eff = 상별
  오버라이드 `exchange_tau_h_per_phase[h]`가 있으면 그 값, 없으면 다-DC(고용체)
  채널은 전역 `exchange_tau_h`, 단일-DC 결정상(CH·AFt·방해석 등)은 0(전량 제공).
  근거: τ는 C-S-H 재결정/교환 시간(문헌 수개월~수년)이고, 결정상 용해·성장은
  표면 제어로 빠르며 CH 유보는 pH 완충을 침묵 파괴한다. 오버라이드 키는
  `Engine.__init__`에서 번들 hydrate_ids와 대조(미지 키 = 하드 에러).
- 급이: offered_mol = f_h × owned_mol (covered-portion 기계는 offered 크기에서
  그대로), offered_env = f_h × own_vol, 백엔드 solid_elements = offered_elem 합.
  방출 스케일 협상(s)은 방출분만 스케일 — offered는 60회 반복 동안 불변(현행 구조
  동일, 메모 키 안정).
- 원장: "빼는 수치 = 먹인 수치"를 offered로 유지 → §6.1 1b(a)(b)는 구성상 성립,
  (c)는 인출이 f×(풀 + 보유 비례)로 유계라 현행보다 안전.
- 풀 갱신: **f=1.0이면 현행 절대 교체 코드 경로 그대로**(비트 동일의 근거 — 대수
  환원이 아니라 분기). f<1이면 잔여 아카이브 R = 풀 − 급이분에 cap =
  min(1, 유보질량/ΣR⁺)를 곱해 parcel_em과 합산 — 풀 질량이 소유 질량을 넘지
  못하는 자기치유 유계. f→0 극한 = 침전 원장(풀은 신규 침전만 누적).
- 유보분 (1−f)는 그 스텝 **불포화에도 불용**(교체 회전율 n/τ가 정의하는 물리;
  탈출구는 상별 τ=0과 모드 부재). 거부→dt 반감 재시도 시 f는 시도 dt에서 재계산
  (순수 함수 — 재시작 결정론 무료).
- τ는 gems3k 백엔드 전용(합성 백엔드는 재평형 급이가 없어 침묵 무효과 — 거부).

#### 4.6.2 모드 C — 서브클러스터 도메인 + 그래프 확산 (`transport.domains`)

- **분할**: 도메인 = 연결 클러스터 ∩ 정적 축정렬 타일. `tile_vox`는 격자 크기의
  약수(= 격자면 자명 분할 — 클러스터 라벨 리터럴 반환). 합성키 = 라벨×T³+타일,
  np.unique 정렬 → (클러스터, 타일) 순 연속 id(결정론). 타일 내 비연결 로브는
  한 도메인 유지(즉시 혼합 폐포가 타일 크기로 유계 — 해상도 노브가 바로 이것).
  분할은 저장하지 않고 재계산(§2.3).
- **그래프**: 노드 = 습윤 도메인(저장량 = 원소 인벤토리 n(D,E), 물 W = 모세관
  액체 합). 엣지 = 상이 도메인의 면 인접 습윤 복셀 쌍(6-이웃 구조상 항상 동일
  클러스터 내부). 면 전도 = 공용 `conductance_field`의 조화평균, 엣지 전도
  G_AB = 계면 면 합, 전달률 **T_AB = D₀·G_AB / p** (타일 피치 p — TPFA;
  타일별 상수장에서 미세 네트워크와 일치 = §6.2 앵커). NETWORK_FLOOR는 물리에
  넣지 않는다.
- **TPFA 근사 한계(외부 검토 2026-08-20 권고 2)**: 전달률 T=D₀·G/p는 도메인
  중심거리를 타일 피치 p로 고정한다. 클러스터∩타일 도메인이 타일 구석의
  슬리버일 때 이 가정은 국소 O(1) 오차를 낼 수 있다(혼합셀 방법의 공통 근사).
  §6.2 해석 앵커 2의 정확성은 "타일별 상수장" 전제 위의 것이며, 실제 적용
  오차의 해상도 노브는 타일 크기다.
- **모드 B의 f 형태(권고 6 기록)**: 선형 f=min(1,Δt/τ)는 1차 정확하며 Δt≪τ
  운용에서 지수형 1−e^(−Δt/τ)과 차이가 없다. τ 실측 보정에서 dt 의존이
  문제가 되면 지수형으로 교체 가능하고, 지수형도 τ≤dt에서 float 정확히
  1.0이 되므로 비트 동일 게이트는 유지된다. τ ≤ dt_min은 영구 f=1(침묵
  전평형 회귀)이므로 config에서 거부한다(권고 1 반영).
- **농도·구동력**: c = n/W (모세관 액체 농도; 겔수는 전도만 하고 저장하지 않는다 —
  cluster_inventory가 모세관 용액 원장인 현행과 정합). **모든 원소 공통 단일
  D₀**(`d0_m2_s`, 기본값 없는 필수 명시 — 이온 자기확산 0.8–5.3e-9 m²/s 범위를
  docstring에 인용): 원소 상태에는 화학종 D가 정의되지 않고(추측 = 발명),
  전하 좌표가 구조적으로 부재하며(Zz 전면 탈락) GEMS가 H/O 기저로 재중화하므로,
  공통 D₀는 국소 중성 용액 조성을 비례 이동시키는 물리적 v1 폐포다.
  종별 Nernst–Planck 영전류 사영은 문서화된 v2 업그레이드 경로.
  **D_rel 피드백 금지**: 그래프가 이미 기하 저항이다 — 곱하면 이중 계상.
- **적분**: 명시적 FTCS는 불가(Δt_exp ≈ 3e-6 h @0.5 µm — 안정 한계
  Δt ≤ min_A[p·W_A/(D₀·ΣG)]). **backward Euler 플럭스 형태**: (diag(W)+ΔtL)c⁺=n을
  Jacobi-CG(rel 1e-12, 고정 연산 순서, 11원소 열 블록)로 풀고, 엣지별
  F=Δt·T·(c⁺_A−c⁺_B)를 **반대칭 적용**(같은 float를 빼고 더함 → 원소 보존 구성상
  정확, injected 무접촉). M-행렬 ⇒ 양성; CG 더스트 음수는 morphology.remove식
  결정론 보정(<1e-11 상대 어서션). 미수렴 = `transport_failure` 거부(dt 반감이
  조건수도 개선). 더스트 도메인(W < 1e-6×습윤 평균)은 엣지 제외(수송 동결).
  BE의 대-Δt 극한 = 성분별 농도 균질화 = 확산이 빠르면 전평형 물리 회복.
- **분할 순서(Lie, T→R, 트라이얼당 1회)**: 라벨 → 분할 → 행수 계약 → **BE 교환** →
  용해 → 귀속 → 도메인별 급이(모드 B의 f⊙는 여기, 교환 후 인벤토리에) →
  더티/예산 선별 → 도메인별 GEM → 배치/물 정산(클러스터 수위) → 재라벨·재분할 →
  리매핑(+fold) → 게이트. 계수(그래프·W)는 스텝 시작 스냅샷 고정. Strang 불채택
  (정확도는 화학 폐포 오차가 지배 — YAGNI).
- **배치·물은 클러스터 수위 유지**: 도메인은 용질·화학 해상도이지 수리(hydraulics)
  단위가 아니다. 공간충전 게이트는 도메인 id 순 **순차 용량 청구**(클러스터 용량 −
  선행 도메인 청구분; 1도메인/클러스터에서 현행 게이트로 정확 환원). trace-water
  판정은 모드 C에서 `water_c < 1e-3 × (총수/습윤 도메인 수)`로 재앵커(모드 부재
  경로는 현행 식 바이트 동일 — 재앵커 없이는 D=10³⁺에서 전 도메인이 trace로
  동결·전역 수화 정지). 리매핑은 액체 겹침 규칙 그대로 + **fold**: 부모 클러스터가
  살아 있는 건조 도메인의 행은 형제 도메인에 신규 액체 부피 비례로 재배분
  (`remap_events` kind="fold"), 클러스터 전체 사망 시 현행 reject/드롭 의미론.
- **GEM 경제**: ① 더티 플래그 — 마지막 평형 이후 원소 L1 드리프트(도메인
  **용매 몰로 정규화** — 평형 인벤토리 기준은 신선수에서 스케일이 붕괴해 임계가
  도달 불가; 따라서 `dirty_rtol`은 몰랄 스케일 허용치로 읽힌다) + 물 상대
  드리프트의 합 r_A > `dirty_rtol`이면 평형, 아니면 **스킵 = 원소 정확**(방출을
  인벤토리에 직접 가산, 조합 동결 — 근사는 상 조합의 시점뿐).
  강제 범주(첫 접촉·리매핑 비항등·경계 결합·동결 이벤트)는 예산 면제.
  ② 연령 상한 `eq_max_age_steps`. ③ 하드 예산 `max_gem_calls_per_step` —
  초과 시 lexsort(−r, id) 상위 선별(드리프트 누적 = 기아 없음). 지표
  n_gem_calls/n_deferred/max_r_deferred/max_age 필수(침묵 노화 금지).
  경제 스냅샷(`domain_eq_*`)은 체크포인트 동승 — 재시작 비트 동일의 전제.
  메모 LRU는 max(256, 4×도메인 수). 유사 도메인 병합 호출은 v1 불채택
  (조성 재스미어링 + 급이 원장 정확성 파괴).
- **주의(탄산화 대비)**: residual_inventory는 gas_gen 원소를 포함한다(봉인계에선
  O2 시드 더스트). 기체상이 유의해지는 응용(탄산화)은 수송 전 gas 분리가 선행
  조건 — RT-W4에서 다룬다.

#### 4.6.3 경계 저수조 (RT-W3, `transport.boundary`) — W3 설계 리뷰(2026-08-20) 확정판

- **의미론**: 저수조 = 노출면의 고정 조성 고스트 = **연속 갱신(무한 희석) 배스** —
  문헌 앵커 프로토콜(Adenot/Mainguy/Haga의 갱신 탈이온수)과 동일. RVE는 미시
  시편이므로 배스는 비커가 아니라 **거시 배스가 시편 표면에 부과하는 경계조건**이다.
  유한 배스 그래프 노드는 두지 않는다: 물리적으로 유의미한 W_res는 RVE 액체의
  ≥10⁴배라 고정-c와 5% 이내 동일하고, 거대 W_res 노드는 CG 수렴 판정(bnorm)을
  오염시키며 음수 보정이 저수조 행을 침묵 손상시킨다. 유한/pH-stat 거시 실험 대응은
  문서화된 확장 = 시간 스케줄 c_res(t)(구간상수). 정적 유한 배스의 고정-c 유효
  기준: η = S·x_f·(A/V_bath)/c_s < 0.05 (S ≈ 4–6 kmol/m³, c_s ≈ 20 mol/m³).
- **위상**: 노출축(선언에서 유도, v1은 1축·low/high/both)은
  `label_clusters(periodic_axes)`와 `build_domain_graph`에서 비주기화 — 주기
  라벨링 + 경계층 결합은 L마다 추출면이 있는 슬래브 배열(실효 깊이 L/2, √t 전선
  파괴)이라 불허. 라벨 비주기화 후 그래프 랩 엣지 제거는 **필수**(잔존 랩 엣지는
  서로 다른 클러스터를 잇는 계약 위반 엣지가 된다). 횡축 2개는 주기 유지(무한
  평판 = 1D 전선 기하). morphology 셸(%n, r=3)·용해 가중(roll, r=1)·**사이트
  귀속 `_neighbor_best_label`과 링 릴레이(engine, roll — 원장 질량을 귀속시키므로
  명시적으로 지명)**의 주기 랩은 잔존 근사로 유지: 영향권 = 양면 각 4복셀 할로,
  건조 사이트 전용이라 포화 용출에서는 퇴화 구성에서만 발화. **유효 조건:
  x_f + w_front ≤ L − 4h** (w_front ≈ max(tile_z·h, 2h)); 층별 관측 피팅은
  노출면 첫 4층 제외. 감지: 층별 CH 프로파일(analysis, W4 추가) — 마지막
  (4+tile_z)층에서 초기 CH 5% 초과 소실 시 리포트 warn
  `boundary_front_breakthrough`(비블로킹). CH 전선 문헌 속도 a ≈ 100–200
  µm/√day(성숙 페이스트; 미성숙은 ~2×까지 빠름) ⇒ 관통 시간 32/64/128 µm ≈
  0.6–2.5 h / 2.5–10 h / 10–39 h. √t 창 확보에는 h=0.5 µm 또는 L≥64 µm 권장.
- **결합**: `boundary_coupling` = 노출면 **습윤**(domain_id ≥ 0) 경계층 복셀의
  반셀 전도 **Σ 2g_a**(공용 `conductance_field`; 네트워크 솔버 gin = 2g_a 규칙),
  전달률은 exchange_be가 엣지와 동일하게 **T_AR = D₀·G_AR/p**를 적용 — 스케일
  지점은 각 1곳, **2를 두 번 곱하지 않는다**(§6.2 슬래브-Dirichlet 앵커가 수치로
  고정; 종전 표기 "T_AR = 2·D₀·G_AR/p"는 G_AR에 이미 든 2와 중복이었다 — 설계
  리뷰 발견). BE에서 기지 c_R은 우변 이동: (diag(W)+ΔtL+Δt·diag(T_bnd))c⁺ =
  n + Δt·T_bnd·c_res — SPD·M-행렬 유지(대각 지배 강화), 배스 행 부재로 음수
  보정과 구조적으로 무관. **내부 엣지가 없어도 배스 결합 도메인은 참여(act)** —
  표면 연결 고립 공극은 물리적으로 용출된다. 더스트 도메인은 내부 엣지처럼 배스
  결합도 제외(수송 동결 정책 일관). 경계 선언 시 tile=격자여도 도메인 기계 활성.
- **c_res 규격**: 단위 = **모세관수 m³당 mol**(내부 c = n/W와 동일 기저; 희석
  배스에서 몰농도×1000과 <1% 일치; 몰랄은 제공하지 않는다 — 솔버가 부피 기저).
  환산 1회: c_vox = c_SI × (voxel_m)³. 순수수 = 전 원소 0(자가해리 10⁻⁷ M은
  공극수 0.1–0.5 M 대비 무시). 용존종은 용매 제외 전 원소를 지니므로 배스도 동일
  규약: NaOH c → {Na:c, O:c, H:c}; Ca(OH)₂ c → {Ca:c, O:2c, H:2c}; Na₂SO₄ c →
  {Na:2c, S:c, O:4c}. **중성 화학식 단위의 합성만 허용**(원소 상태에 전하 좌표가
  없어 기계 검증 불가 — Na 단독은 금속 나트륨이 된다). 미지 원소 키 = 하드 에러.
  건조 면 복셀은 결합하지 않는다(수분 유출 제외 유지 — 재포화는 범위 밖).
- **원장**: 플럭스는 같은 float로 도메인 행 차감 + `boundary_exchanged_elements`
  누적(+ = 계 내로; 순수수 용출의 Ca는 음) — §6.1 기대변이 이미 포함하므로
  **원장·게이트 변경 0**. boundary_net과 Σ도메인 Δ의 일치는 float 재사용 수준이며
  감쇄 순서 더스트는 기존 `balance_exchange` 공차가 흡수(비트 동일을 약속하지
  않는다). `boundary_water_mol`은 W3에서 항상 0(용질 전용; 물 항등식은 이미 이
  항을 읽는다 — "활성화"가 아니라 비영값 대기).
- **dt 정책(용출 응용의 전제 — 설계 리뷰 핵심 발견)**: RVE 척도에서 BE 수송은
  dt ≥ 분이면 스텝당 클러스터 전체를 c_res로 이완시키므로, Lie 분할의 스텝당
  유출 상한 = 용액 인벤토리 1회분이다. 이를 무시하면 전선이 배스 공급 제한 —
  **선형 t, dt 의존 — 이 되어 √t 검증 관측 자체가 파괴된다**. 충실 조건:
  **dt ≤ (φ·L·x_f/D_e,deg)/2**; 전 관통 ≈ Γ/ρ ≈ **1,000–2,000 스텝**(Γ =
  S/(φ·c_s) ≈ 500–1,200, L·h 무관). 권장 dt: 32 µm 5–20 s / 64 µm 20–60 s /
  128 µm 1–5 min. 지표 `boundary_supply_ratio`(스텝 유출 ÷ 배스 연결 용액
  인벤토리) 지속 > 0.7 = warn. 전도장 1스텝 지연은 이 dt에서 층당 ≥8스텝이라
  흡수되고 용출 방향(공극 개방)에선 보수적. 초기 전 인벤토리 알칼리 플러시는
  물리적이며(32–128 µm 슬래브는 ≲1 h에 세척) 가드 밴드가 제외; 세척 후
  c_s(CH)가 ~2 → ~22 mol/m³로 뛰는 천이도 √t 피팅에서 제외할 것.
- **국소 물 고갈 동결(W4 실측 반영)**: 모드 C에서 협상 소진형
  insufficient_water(도메인 평형이 자기 물로 어떤 방출 스케일에서도 불가)는
  거부가 아니라 **도메인 동결**이다 — snapshot 델타는 dt 무관이라 거부는 런
  중단뿐이고, 이는 공간충전 동결과 동일한 물리(국소 물 결핍 포켓의 수화 정지).
  후기 수화의 밴드 타일에서 실측 발생(t≈126 h, 16밴드 32³). full 모드는 클러스터
  전체 물이 풀링되므로 하드 거부 유지(그곳의 실패는 결함 신호). 지표
  `water_frozen_domains`.
- **폭기 갱신수 권장(W4 실측 — 산화환원 앵커)**: 순수수 용출 배스는
  **{"O": 0.5} mol/m³(용존 O₂ ≈ 8 mg/L, 공기 평형수)**로 선언하라. 물리적으로
  갱신 탈이온수 실험의 실제 상태이고, 수치적으로는 배수(排水)가 초기 O₂ 시드를
  씻어낸 뒤 남는 산화환원 미정착이 GEM/AIA의 확률적 비수렴(실측: 스텝당 최대
  17/29 도메인 동결 → 전선 정체)을 유발하는 것을 수송이 앵커를 상시 재공급해
  차단하기 때문(실측: 동결 8/스텝 → 1/스텝, CH 전선 즉시 전진). 진공/혐기 배스
  응용은 별도의 산화환원 전략이 필요한 문서화된 한계.
- **비수렴 플러시-재시도(W4 실측)**: 배스 클러스터 도메인의 GEM 비수렴 시 1회
  한정, 잔여 용질 전량을 경계 원장으로 플러시(정확 float 이동)하고 재시도 —
  격리 실험이 "인벤토리 정확히 0이면 수렴"을 입증했기 때문. 폭기수 채택 후에는
  드물게만 발화(안전판). 지표 `bath_flushed_domains`. 주의: 플러시-재시도 유발
  O₂ 시드/플로어 주입 누적이 injected 상한을 압박할 수 있으므로(실측: 플러시
  140회에 상한 도달) 폭기수 없이 이 경로에 의존하지 말 것.
- **배스 스윕 플로어(W4 실측 반영)**: BE 배수는 배스 연결 클러스터의 용질을
  스텝 시작량의 4자릿수 이상 아래 미량(실측 1e-19~1e-155 mol)으로 남기고, 이를
  GEM에 먹이면 AIA가 대량 비수렴(실측: 스텝당 ~17/29 도메인 동결 → 전선 정체)한다.
  갱신 배스의 물리 = 마지막 미량도 가져간다: 교환 후 배스 연결 클러스터에서
  **몰랄 플로어 1e-8 mol/mol-water(≈6e-7 mol/L)** 미만 항목을 경계 원장으로
  플러시(같은 float 이동 = 폐합 정확). 플로어는 물 기준이라 상태 무관·부트스트랩
  안전하며, 자가해리(1e-7 mol/L) 곁·평형 재충전(~1e-2 mol/L)의 5자릿수 아래·
  실측 배수 잔재의 위에 놓인다. 지표 `bath_swept_mol`.
- **배스 앵커 시드의 경계 기장(W4.1 실측 반영)**: 공급 제한 배수 전선 도메인은
  매 GEM 콜마다 워커의 산화환원 시드를 다시 받는다 — 미세 dt에서 콜당
  ~1e-19 mol 더스트가 스텝 수에 선형 누적해 injected 상한(1e-6·scale)을 치고
  런을 중단시킨다(실측 dt=0.5 s, D0=5.3e-9: 성장 2.2e-16→1.5e-15 mol,
  t=168.99 h 중단 — 실질 척도의 13자릿수 아래). 배스 결합 도메인이 받는 앵커의
  물리적 출처는 폭기 배스의 용존 O₂이므로 **배스 결합 도메인의
  `result.injected_elements`는 `injected_elements` 대신
  `boundary_exchanged_elements`(+유입)로 기장**한다(정확 float; 폐합 항등식은 두
  항 모두 가산원이라 무손상). injected 게이트는 밀봉 의미(솔버 위조 질량 감시)를
  유지하고 밀봉 런·비배스 도메인은 무변경. 지표 `bath_anchor_mol`.
- **비수렴 동결(구현 중 실측 반영, W4에서 범위 확정)**: 분할된 도메인은 풀링된
  클러스터가 결코 겪지 않는 국소 극단 조성에 도달한다 — 배수된 용액(조합 +
  준순수수 + 1e-19 더스트; 실측: 1e-8 상대 섭동이 수렴을 가른다)과 후기 수화의
  물 결핍 밴드(실측: 밀봉 16밴드 32³, t≈126 h) 모두 방출 스케일·dt로 해소되지
  않는 GEM/AIA 칼날 경계다. 따라서 **모드 C(도메인 분할 활성) 런에서** 지속
  비수렴 도메인을 공간충전 불가능성과 동일하게 동결(방출 → unmet, 조합·인벤토리
  유지)하고 이후 스텝이 조성을 재정칙화하게 둔다. **full 모드는 하드 거부
  유지**(그곳의 비수렴은 결함 신호 — 1-도메인 퇴화 게이트가 이 안전망을 보존).
  지표 `nonconv_frozen_domains`; 경계 런에선 `boundary_supply_ratio` warn과 함께
  읽을 것.
- **고아 포켓 dryout 인계(W4.1 실측 반영)**: 밀봉→노출 전환의 비주기화는 랩으로
  연결돼 있던 포켓 클러스터들을 고아로 만들고, 이들은 자기 평형이 마지막 물을
  결합수로 가져가며 **한 스텝에 라벨링 문턱 아래로 자가건조**한다(실측 32³:
  t≈168.2 h에 행렬적 소멸, n_dom 100→30; dt 무관 — dt_min 바닥에서 결정론적
  동일 시행 반복 → 9회 거부 중단). 행이 정확히 0인 포켓은 조용히 소멸하지만
  부동소수 더스트(실측 ~2e-20 mol, 풀의 ~1e-7)를 실은 포켓은 크기 하한 없는
  dryout 판정에 하드 거부된다. 규칙: **배스 활성 모드 C에서, 전 클러스터 사망의
  행은 이중 증인 — Σ|row| < 1e-3·Σ|전체 용질 풀| 그리고 부모 클러스터 스텝 시작
  물 < 1e-3·총 물 — 을 모두 만족할 때만 경계 원장으로 인계**(`boundary -= row`,
  플러시 선례의 정확 float 이동 = 폐합 항등식 무손상; 드롭이 아니다). 하나라도
  실질이면 v2 하드 거부 유지. 실측 여유: 더스트 사망 1e-7~1e-6 대 실질 행(침투
  클러스터 밴드, 전체 사망 불가) 6e-5+, 포켓 물 7e-7 대 침투 1.0 — 양쪽 3자릿수.
  밀봉 모드 C·full 모드는 무변경(인계처가 없고, v2 계약 유지). 지표
  `dryout_surrendered_domains`/`dryout_surrendered_mol`.
- **경제·비용**: 배스 결합 도메인 = 강제 범주(§4.6.2 기존 명시; 구현 = T_bnd > 0).
  정육면체 타일은 강제 호출 (N/tile)²/면 — 평면 전선은 횡방향 균일이므로
  **축별 타일 (tile_z, tile_y, tile_x)**(횡 = N, 축 = 2–4)이 W4의 지배적 비용
  레버이자 더 나은 물리다(강제 ~1밴드 + 전선 밴드 수 개 → 스텝당 ≤~25 호출).
  W4 착수 시 config 확장으로 구현한다.

#### 4.6.4 Tier 0 — 종별 NP 영전류 사영 (RT-P0, `transport.domains.species`)

부속 설계문서 `TINN_RT_phreeqc_tiers_spec_2026-09-01.md` §2의 확정 병합본.
모드 C의 스칼라 `d0_m2_s`를 수용액 스페시에이션 + 종별 자기확산계수 기반
**에지·원소별 유효 전도도**로 대체하는 config 선택형 옵션. PHREEQC 코드는
실행되지 않는다 — phreeqpython 동봉 `phreeqc.dat`의 `-dw` 57건을
`gems_bundles/species_dw/species_dw.json`으로 벤더링(생성기
`scripts/make_species_dw_table.py`, PROVENANCE 동봉; alias는 GEMS DC명 →
phreeqc명 항등 매핑만, 미매핑 종은 `default_dw_m2_s` **명시** 시에만 그 값
+ 런 summary 목록, null이면 거부 — 침묵 기본값 금지).

- **데이터 경로**: 워커 프로토콜 v2 — info()에 `species_charge`(DCH Zz 열),
  평형 응답에 `aqueous_species_mol`(전 수용액 DC mol; 기존에 계산 후
  버리던 dict). 구버전 워커 + species 활성 = 거부(`require_speciation`),
  비활성 = 무시(하위호환 `.get` 패턴).
- **동결 스페시에이션**: 도메인별 종 mol 행렬 `domain_species_mol (D,S)` +
  런 스코프 `aq_species_ids`(FORMAT_VERSION 5, §2.3). T는 직전 R의
  스페시에이션(리매핑 경유) 위에서 돈다 — Lie 1차 정합. **보존 원장이
  아니라 계수 캐시**: §6.1 폐합 불참, 물 비례 리매핑, dryout 행 폐기,
  미평형 도메인 = 제로행 → 해당 에지 전도 0 + `np_empty_el` 보고.
- **수식**: 에지 (a,b), 동결 c_i (= n_i/W), c̄_i 조화 면평균(배스 면은
  상류 one-sided — 조화면 이동항이 항등 소멸), Φ* = Σz_jD_jΔc_j /
  Σz_j²D_jc̄_j, 원소별 `D_eff = (Σν_iD_iΔc_i − Φ*·Σν_iz_iD_ic̄_i) / Δc_el`
  — 동결 상태에서 NP 영전류 원소 플럭스와 항등. BE는 원소 공간 유지,
  기존 블록 CG를 열 축으로 재사용(스텝 내 비선형 재결합은 비범위 = P0c).
- **클램프 사다리**(각 단 카운터 보고): ① |Δc_el| 미소 → 가중 Fick D̄_el
  ② 본식 ③ 음수 → Φ 항 제거(순수 Fick) ④ 여전히 음수 → D̄_el ⑤ 에지 내
  전 종 최대 D 상한. 0 ≤ D_eff ≤ D_max 무조건 → M-행렬·무조건 안정·
  플럭스 반대칭(구성적 보존) 전부 유지.
- **제약**: gems3k 전용; `d0_m2_s`와 상호 배타(diagnostics_only는 d0 필수
  — 스칼라 물리 + 리포트만); 배스는 O/H(폭기수)만 허용(용질 배스의
  스페시에이션은 미정의 — 재검토 트리거: 이온성 배스 응용 착수);
  온도 보정은 Stokes–Einstein 1차(0–100 °C 점도표), 계수 summary 기록.
- **RT-P0a 실측(2026-09-02, OPC 32³ 6 h)**: D_eff/D₀ 분포 0.008–8.1×,
  p50 1.57× — 검토서 "이온성 매질 단일 D₀ 2–4×"가 계기화됨.
- **P0c(종 공간 완전 결합)는 이연**: P0b 실측에서 클램프율·오차가 유의할
  때만 별도 설계 리뷰로.

#### 4.6.5 Tier 1 — PHREEQC 수착 연산자 (RT-S1, `sorption`)

Lie 분할 T→R을 **T→S→R**로 확장. S = 도메인별 순수 함수: (공극수 원소 +
직전 수착 재제공 + 물 + 사이트 총량) → 평형 표면 결합 벡터 (snapshot
의미론 — 사이트가 줄면 자동 탈착). 구현은 IPhreeqc in-process(SOLUTION +
SURFACE만 — **상 조합 권위는 GEMS**, EQUILIBRIUM_PHASES/SOLID_SOLUTIONS
금지), DB는 벤더링 cemdata18.dat(호출 전후 sha256 감사).

- **수착 화학은 config 소유**: cemdata18.dat에 SURFACE 정의가 없음(실측
  grep 0건) — 표면 반응식·log_K·`sorbed_elements`(결합종 1 mol당 용액에서
  제거되는 원소 행)를 config에 명시. 연산자는 매 호출 그 행을 PHREEQC
  자신의 용액 수지와 대조해 불일치 시 거부(폐합 증인, rel 1e-8).
- **사이트 부기(endmember 보존, 사용자 결정 2026-09-02)**: 급이와 동일한
  covered-pool + 전역 fallback endmember 분할을 **전체 소유량**에 적용
  (모드 B의 노화 분율과 무관 — 표면은 존재 자체가 사이트),
  `site_density_mol_per_mol`은 CSHQ endmember별 필수 명시.
- **S 단계 wetness 계약 (2026-09-02)**: 반응기가 수용액인 조건 = 물/용질
  몰비 ≥ 10 (무차원·intensive; 실제 공극수 >50, 리매핑·접기 float dust는
  ~1). 미달이면 water≤0 분기와 동일하게 저장소 동결 + 스텝 지표
  `sorption_dry_reactors`로 보고(침묵 아님). dust 조항: 용질 총량이 원장
  노이즈 플로어(`1e-24 + 1e-12 × max|inv|`, overdraw 가드와 동일 스타일)
  이하인 오퍼도 동결 — 화학계가 아님. 실측 근거 2건: ① 물 5.6e-17 mol /
  S 7.3e-17 mol dust가 스케일 후 ~7e4 mol/kg "용액"이 되어 IPhreeqc
  A(H2O) 발산, ② 물-과잉·용질-노이즈 반응기가 산화물 분해기의 정확 폐합
  검사를 노이즈 크기에서 격발(O 잔차 −4e-16).
- **원장**: `domain_sorbed_mol`(v5 예약분 활성) — §6.1 폐합이
  "수용액+고체+**수착**=전체"로 확장, S 단계 이동은 같은 delta 배열로
  양쪽에 기입(`balance_sorption`, 위반 = 부기 손상). 리매핑은 인벤토리
  가중, dryout 접기 이벤트 기록, hard dryout = 스텝 리젝트.
- **정준 스케일링**: RVE 원장(~1e-10 mol)에서 IPhreeqc 절대 수렴 기준이
  실패(실측) → (용액, 물, 사이트) 공동 스케일 1e-2(평형은 intensive,
  GEMS 백엔드 전례) 후 정확 역스케일. 스케일 앵커는 **용액 peak와
  사이트 총량의 기하평균** (2026-09-02 보강: 문헌 밀도에서 사이트가 용액
  원소를 1e2–1e4× 초과하는 초기-수화 클러스터가 실재 — 원소-only 앵커는
  표면을 0.62 mol로 올려 표면/H/O 수지 발산, max() 앵커는 용액을 회피
  대상이던 ~1e-8 영역으로 되눌러 A(H2O) 발산, 둘 다 실측. 기하평균이
  편차를 대칭 분할하며 공동 인자라 사이트/용액 비 — 물리 — 는 정확).
- **제약**: gems3k 전용, 용질 화이트리스트(첫 대상 SO4/S — Cl은 E3 염
  담체·배스 확장 후속), Na/K는 `alkali_exchange` 명시 + 비-CNASH 번들
  (구조 흡수와 이중 계상 금지), ddl은 실전 정전 파라미터 확보 전 거부.

**실측 기록 (RT-S1c, 2026-09-02 — SO4 문헌 보정 + 28d 실측)**: 사용자
공급 문헌 5편에서 추출·보정.

- **사이트 밀도 (구조 기반, 피팅과 독립)**: Labbez et al. 2006 (JPCB
  110, 9219) 티트레이션 실란올 **4.8 /nm²** × Divet C-S-H BET SSA
  350 m²/g (C/S 1.57, H/S 1.26 → 170.8 g/mol-Si) = 2.79 mmol/g =
  **0.4766 mol 사이트/mol Si**; endmember별 = ×Si 화학량론(PC 번들
  DCH: TobH/JenH 1, TobD/JenD 0.6667) → **TobH/JenH 0.4766,
  TobD/JenD 0.3178**. 교차: Haas & Nonat 2015 (CCR 68, 124) 몰당
  titratable 실란올 0.5–1.0/Si — 동일 차수; Divet 등온선 plateau
  접근(~2 mmol/g @150 mmol/L)과 정합.
- **log_K (등온선 피팅)**: Divet & Randriambololona 1998 (CCR 28(3),
  357) 배치 실험(3.4 g C-S-H/250 mL NaOH, 25 °C)을 **SorptionOperator
  자체로 재현**(cemdata18 활동도, no_edl, 사이트 고정)해 0.1 M NaOH
  등온선(Fig. 3 디지타이즈 11점, OPC 공극수 최근접 pH)에 최소자승 →
  **log_K = +0.50 (−0.10/+0.05)** (0.25 그리드 + 0.05 정밀화, SSE 0.286
  (mmol/g)², RMS ~0.16 mmol/g; 괄호는 SSE 평탄역 — 0.40–0.52에서
  SSE +1% 미만). **불확도 사슬 주의**: 사이트 밀도는 BET 방법 의존
  (Divet 자체 320–350 m²/g)과 silanol/Si의 Ca/Si 의존(Haas & Nonat의
  γ-C-S-H는 0.5/Si — 고 Ca/Si 조성이 낮은 쪽) 탓에 **×1.5–2 밴드**를
  가진 점추정이다. 파생 Rd(10 mmol/L, 0.1 M NaOH) ≈ **29 L/kg** —
  Ochs et al. 2016의 약한 음이온(Se(VI) 유사체) C-S-H 밴드와 정합.
  피팅 스크립트+디지타이즈 점 = `scripts/fit_so4_logk.py`(재실행 가능,
  스케일 불변 실측: 기하평균 앵커 전환 후 12자리 일치).
- **모형 한계 (메커니즘 표 항목)**: 0.5 M 검증 — 논문 자체 회귀(Fig. 2:
  1/Cb = 54.42/C − 0.053, R² 0.9868) 대비 모형 ~6× 과소. 단일 리간드
  교환(OH⁻ 방출)의 pH 경향이 실측(pH·이온강도와 함께 **증가**)과 반대 —
  상수는 보정 pH(~12.9–13.4) 전용, pH 외삽 금지. **바인더 결합 주의**:
  28d 런의 보정창 일치는 현 qualification 바인더가 알칼리-빈약(아카나이트
  0.5%뿐)해 공극수가 pH ~13.0에 앉는 덕이다 — 실제 알칼리 함량 바인더
  (Na₂O-eq ~1%)에선 운영점이 pH 13.3+로 이동해 이 한계가 실제 오차로
  전환된다.
- **RT-S2a — ddl 능력 + 실측 판정 (2026-09-02)**: `surface_model: "ddl"`
  구현 — config가 `specific_area_m2_per_mol_site`(ddl 필수/no_edl 금지,
  Labbez×Divet = 1.2544e5 m²/mol-사이트)와 `charging_reactions`(실란올
  탈양성자화·Ca 착화; 결합종·원소행이 수착 반응과 **동일 기계** 탑재,
  H⁺ 방출도 같은 원장 delta)를 소유. 면적은 사이트와 공동 스케일(전하
  밀도 σ가 intensive). `bound_species` 파서를 항 구분자 기반으로 수정
  (하전 종명 `Surf_sOCa+` 보존). 분해기에 NaCl 담체 추가(Cl은 원장
  원소가 아직 아님 — E3 선행분, Na 부족 시 거부). 신규 optional은 해시
  불가시(pre-S2a 수착 config 해시 불변, 테스트 고정).
  **공동 보정 실측** (`scripts/fit_so4_ddl.py`: NaOH 4곡선 33점 피팅 +
  NaCl 2곡선 out-of-sample 예측; 리뷰 정정 2026-09-02 — 0.5 M 곡선을
  Fig 2 회귀의 고농도 외삽에서 **Fig 3 실측 디지타이즈로 교체**, 외삽은
  C≥80 mmol/L에서 Cb를 ~30% 부풀렸었음): 최적 (A형 OH방출, pK_H
  9.8/K_Ca −7, Na쌍 0.0, SO4 log_K +5.1)에서도 joint RMS 0.65 mmol/g —
  pH 사다리를 정량 재현 못함(0.05 M ~2–4× 과대, 0.5 M ~5× 과소;
  Gouy-Chapman의 고이온강도 스크리닝 과장). NaCl 경향은 **부호 재현**
  (no_edl은 불가)하나 크기 ~1.5–2× 과대. **판정: SO4 프로덕션 상수는 no_edl 국소 보정
  (S1c) 유지; ddl은 능력으로 탑재하되 정전 지배 이온(Cl 배제, 알칼리
  Donnan)용으로 보류** — 메커니즘 표 항목.
- **28d 실측 런** (`scripts/run_sorption_so4.py calibrated`, Deschner
  OPC 32³ 봉인 dt 0.6 h, 296.15 K, 벽시계 766 s,
  `runs/sorption_so4_results_calibrated.json`): 수착 S 24 h 1.68e-12 →
  168 h 1.75e-12 → **672 h 1.85e-12 mol = 총 S(1.665e-11)의 11.1%**
  (위 불확도 사슬을 통과시키면 ~5–20% 밴드의 점추정 — 밀도 ±2× 민감도
  런은 미실행);
  사이트 1.91→3.85e-11 mol(CSHQ 성장 동승), 점유율 4.8%, 커버리지
  0.999→0.938. 최종 공극수(주 클러스터): **[SO4] 0.37 mmol/L —
  Barbarulo & al. 2007 (CCR 37, 1176)의 20 °C 에트린자이트 평형
  ~0.4 mmol/L과 일치**(sanity 앵커), [K] 114 mmol/L(아카나이트 유래),
  [Ca] 6.3 mmol/L. 수착의 91%가 첫 24 h(석고류 용해 전이기)에 발생 —
  snapshot 재제공이 이후 자동 재평형. 자기일관: 최종 상태를 연산자에
  재제공 시 저장소 0.06% 이내 재현(= 연산자-평형 상태 실측).

---

## 5. 비기능 요구

- Python ≥3.11, 코어 의존성 numpy+pydantic만. zarr/h5py 불요(자체 Zarr-v2 writer).
  gems 모듈만 xgems(pybind11)를 지연 import — 미설치 시 다른 전 기능 정상.
- 결정론: 동일 config+seed → 동일 결과. RNG는 `numpy.random.Generator(PCG64)` 상태 전체 저장.
- 성능 목표: 32³ 합성 런 < 5분, 64³ < 1시간 (일반 데스크톱). 미달 시 기능보다 프로파일링 우선.
  gems3k 전평형은 지속 워커+입력해시 메모이제이션 기준 (실측: 32³ 블렌드 28일 2.2분);
  장기(360일) 연구는 dt_initial 12h 권장. rev.2 프로파일링(90일 32³, 72클러스터,
  스텝 23.2 s): 워커 86%(그중 번들 재파싱 110 ms/호출이 지배), 감사 5%, P&K 재적분 6% →
  서버 엔진 캐시(요청별 clear/cold_start 리셋 — 비트동일 실측+persistent≡spawn 테스트 게이트),
  감사 sha stat-서명 캐시, alpha_at 고정 그리드 프리픽스 메모로 **스텝 5.7 s (4.1×)**.
  스폰 경로는 불변.
- 플랫폼: Windows 우선 (`py` 런처), 경로 구분자 하드코딩 금지.
- 오류 정책: 모르는 상/성분/단위/basis는 추측하지 않고 거부. 침묵 폴백 금지.
- v4.0/RT 예산: GEM 호출은 도메인 수에 비례하므로 모드 C의 비용 지배항이다 —
  기본 분할은 굵게(tile = 격자/4, ≤64 도메인), 호출 예산·더티 스킵이 조절 레버.
  수송 연산자 자체(분할+그래프+BE)는 스텝당 ~0.1 s(64³)–0.4 s(128³, K≈4×10³)로
  GEM(10–60 s/스텝)의 1% 미만이어야 한다. 스텝 시간 비율은 W2+ 게이트마다 기록.
  **RT-W2b 실측 (2026-08-20, OPC PC 32³ 24 h·dt 2 h·12 스텝, tile 8 ≈ 68
  도메인)**: full 16.8 s / 모드 C dirty0 48.3 s(2.9×; α·CH는 full과 소수 5자리
  일치 — 봉인 OPC 단일 퍼콜레이션 용액에서 분할 자체는 화학을 바꾸지 않음이
  실측됨) / 경제(dirty_rtol 3e-3·age 16·budget 32) 36.1 s(dirty0 대비 −25%),
  선택 511·유예 299(37% 스킵)·**동결 16건(dirty0에서는 0건)** — 외부 검토
  권고 3이 예측한 지연-해동 침전 버스트의 실측 확인. 정확도 비용: α −0.08%p,
  CH −4.4%(24 h). 결론: dirty_rtol은 낮게(≤1e-3 권장) 시작하고 동결 카운트
  (`n_frozen_domains`)를 유예 지표와 함께 감시할 것; 3e-3은 CH 민감 관측에는
  과하다.

---

## 6. 최소 실험값·검증 세트 (이것이 전부이며, 추가하지 않는다)

### 6.1 수치 보존 (블로킹 — 유일하게 진행을 막을 수 있는 검증)

| 항목 | 기준 |
|---|---|
| 성분/원소 수지 | \|e\| ≤ atol + 1e-8·\|M\| (글로벌 float64 원장) |
| 복셀 점유 항등식 | 전 복셀 오차 ≤ 1e-12, 음수·클리핑 없음 |
| 물 수지 | 자유+겔+결합 = 총량, 동일 rtol |
| Endmember 폐합 (v3.0/E1) | 채널별 Σ endmember_mol = hydrate_mol **및** Σ(endmember_mol × DCH 원소행) = hydrate_elements_ch, 원소 수지와 동일 허용오차 — 양쪽 다 같은 백엔드 응답에서 누적되므로 위반 = 부기 손상 |
| Endmember 비음수 (v3.0/E2, 1b(c)) | endmember_mol ≥ −(atol + rtol × 글로벌 보유 스케일) — 초과 인출(overdraft)은 폐합 항등식의 양변을 함께 움직여 (a)/(b)로는 불가시(E2 리뷰 발견), 이 검사가 유일한 증인. 소멸 더스트(~eps × 회전량)는 글로벌 스케일 참조로 통과 |
| 원소 폐합 기대값 | initial + injected + boundary_exchanged(RT-W3부터 비영 활성) |
| 배치 수지 | 백엔드 부피 = 요청 = 배치 부피 (허용오차 내), 손실·이동 없음 |
| 롤백 불변성 | 거부 트라이얼 후 커밋 상태 해시 불변 |
| 재시작 동등성 | 중단/재시작 = 무중단, dense 배열·원장·RNG 비트단위 동일 |
| 도메인 분할 정합 (v4.0/RT, 모드 C) | 도메인은 클러스터의 세분: 습윤 복셀 전부 도메인 ≥ 0, 도메인별 부모 클러스터 유일, 건조 = −1 (정수 정확) — `balance_domain:partition` |
| 수착 수지 (Tier 1/RT-S1) | S 단계의 용액→수착 이동은 같은 delta 배열로 양쪽 기입: \|Δinv + Δsorbed\| ≤ atol 1e-24 + rtol 1e-12·Σ\|이동\| — 위반 = 부기 손상. 원소 폐합은 수착 저장소 포함 (`balance_sorption`) |
| 교환 수지 (v4.0/RT, 모드 C) | Σ_도메인 Δinv_교환 = boundary_net (봉인계 = 0), atol 1e-24 + rtol 1e-12·Σ\|q\| — 엣지 반대칭 적용이라 위반 = 부기 손상. `balance_exchange` |
| 물 항등식 (v4.0/RT 확장) | 자유+겔+결합 = 초기 + `boundary_water_mol`(RT-W3 전까지 0) |

### 6.2 재현 앵커 (회귀 테스트 — 문헌/기측정 값과의 일치)

| 앵커 | 값 | 용도 |
|---|---|---|
| P&K 파라미터 표 | Elakneswaran 2018 (doi:10.3390/app8122597) 발표 표 그대로 | kinetics 단위 테스트 입력 |
| P&K 변형 프리셋 | Kulik 2021 / CemGEMS 문서 값 | 제2 프리셋 테스트 |
| OPC 레시피 | NIST SRM 114q: C3S 60, C2S 14, C3A 7, C4AF 10, 기타 9 wt%; Blaine 381.8 m²/kg | 표준 4상 픽스처 (9%는 미배정 유지, 발명 금지) |
| GEMS 앵커 1 | PC 번들 저장 DBR 재평형 → pH 13.596951, I 0.366219 molal | gems 어댑터 회귀 스냅샷 |
| GEMS 앵커 2 | 1.0 g C3S + 0.5 g H2O + O2 시드 → pH ≈ 12.66, 주생성상 CSHQ+Portlandite | gems 어댑터 회귀 스냅샷 |
| GEMS 앵커 3 | CNASH 번들(기본) OPC SRM114q 1d 프로브 → pH 12.661534, I 0.055051 molal, 주생성상 CNASH+Portlandite+C4AH19+C3(AF)S0.84H | CNASH 번들 회귀 스냅샷 (2026-07-16 실측) |
| C3S 픽스처 | 1.0 g C3S, w/C3S 0.50, 293.15 K, 표면 스케일링 중립(1.0) | 최소 스모크 조건 |
| RT 해석 앵커 1 (v4.0) | 2-도메인 교환: Δc(t)=Δc(0)·e^(−λt), λ=(D₀G/p)(1/W₁+1/W₂); BE 1스텝은 Δc⁺=Δc/(1+Δt·λ) 대수 정확 | exchange_be 단위 테스트 |
| RT 해석 앵커 2 (v4.0) | 타일별 상수 슬래브 체인: 도메인 그래프 정상 플럭스 = 미세 격자 `relative_diffusivity_network` 플럭스 (CG 공차 내 — TPFA는 타일별 상수장에서 정확) | 전달률 p-나눗셈·공용 전도 규칙 고정 |
| RT 해석 앵커 3 (v4.0/W3) | 슬래브 + 양끝 Dirichlet 배스: 도메인 그래프 정상 플럭스 = 네트워크 솔버(gin=2g_a) 플럭스 — G_AR 반셀 factor-2의 수치 고정(중복 곱이면 정확히 2× 실패) | 경계 결합 스케일 고정 |
| RT 해석 앵커 4 (v4.0/W3) | 1-도메인+배스 BE 1스텝: c⁺−c_R = (c−c_R)/(1+Δt·λ_R), λ_R = D₀·G_AR/(p·W); 서브스텝 → exp(−λ_R t) 1차 수렴 | boundary exchange 단위 테스트 |
| RT 해석 앵커 5 (Tier 0/P0) | Nernst–Hartley 2종 전해질: D_salt = (z₊−z₋)D₊D₋/(z₊D₊−z₋D₋) 정확 재현(rel 1e-10; NaCl≈1.61e-9·KCl≈1.99e-9 교차 확인) + 등-D 항등(Φ≡0, rel 1e-12) + 에지 전하플럭스 영(1e-12) | `np_effective_conductance` 단위 테스트 |
| RT-W0 기준 앵커 A (실측 2026-08-20, f7be88b+aqueous 포트) | `examples/c3s_32.json` → runs ckpt_003 (t=168 h): dense_hash 1490c624221ef5a109958815989faea840582cc8f543a7e5e2c3fb500d67a6df (버전 불변 권위), full_hash v3 원기록 1c7d2a10…, v4 재고정(RT-W2, boundary_water_mol 편입) fe100126…, **v5 재고정(RT-P0b 2026-09-02, domain_species_mol/aq_species_ids/예약 domain_sorbed_mol이 해시에 편입) 40e4760ed020fe619bc853beba9a91f37de108da1a93ee247fc1d4fb1129de12** — dense 동일 실측으로 물리 불변 입증 | 합성 경로 기본값 불변 감시 (GEMS 불요) |
| RT-W0 기준 앵커 B (실측 2026-08-20, 동일 빌드, xgems py313) | `examples/qualification/deschner_opc_q32_smoke_24h.json` → ckpt_001 (t=24 h): dense_hash 26df94e710619e5f116a607411a5d4197327c4a678b4effbcd1254eb70bfb6e1 (버전 불변 권위), full_hash v3 원기록 84751b8b…, v4 재고정(RT-W2) b30c6f1a…, **v5 재고정(RT-P0b) 78eeec4ef1d6b0ea86791edc62eaed63c97410d873aef7b89a5a6ddcc625e70d** — dense 동일 실측 | GEMS 결합 경로 기본값 불변 감시. 주의: 앵커 실패·이중런 등가 통과 = 환경 드리프트이지 회귀 아님(§3 RT 게이트의 권위는 동일 세션 이중런) |

### 6.3 Sanity band (비블로킹 — 리포트에 pass/warn/info만 기록; info = 해당 배합에 밴드 전제가 비적용, 값만 표시 — rev.2)

M4 이후 OPC 런의 자동 판정 항목. 벗어나면 warn으로 기록하고 계속 진행한다.
목적은 "물리적으로 터무니없지 않음"의 확인이지 과학적 검증이 아니며, 리포트에 그렇게 명기한다.

| 관측치 | band (20 °C, w/c≈0.5 OPC 문헌 통상 범위) |
|---|---|
| 총 클링커 alpha @ 1 d / 7 d / 28 d | 0.25–0.55 / 0.50–0.80 / 0.65–0.90 |
| C3S alpha ≥ C2S alpha (모든 t) | 순서 위반 없음 |
| CH(Portlandite) 존재 | 1일 이후 클링커 잔존 시 CH > 0 (v2.2: 포졸란 소모로 인한 감소는 정상 물리) |
| 모세관 공극률 | 단조 감소, 최종 < 초기 액체분율 |
| 화학수축 진단 @ 28 d | 0.03–0.08 mL/g(반응 시멘트) 자릿수 — **OPC 기준 밴드**. rev.2 원인 규명(2026-07-17, ε=0.384 검증 런으로 실증): 수축(기체 생성)은 Δgas = V_용해 + V_결합수 − V_골격의 순수 열역학 항으로 **ε_gel과 무관**(외피 성장분 = 겔수 이동분이 정확 상쇄). FA30 실측 0.125 mL/g의 초과분은 (a) 포졸란 반응 고유의 높은 수축(문헌 FA ~0.10–0.16 mL/g 반응분)과 (b) 번들 몰부피(C4AH19 등)에서 오며, InverseGems 0D 부피 수지와 정합. **rev.2: SCM 반응 질량 비중 > 10%면 판정 대신 status=info로 값·비중만 표시** — SCM별 수축 계수의 인용 가능한 문헌 범위를 확보하지 못했으므로 발명하지 않는다(확보 시 배합 가중 밴드로 승격). 한편 ε_gel 캘리브레이션(ε(CNASH)=0.384 명시)은 수축이 아니라 **공극률·수송 앵커**로 가치가 실증됨: 28d 모세관 φ 0.338→0.300(실측 범위), D_rel 0.124→0.080(GB 경험식 0.055에 근접), pH 밴드 통과. 기본값은 해시·재현성 계약 때문에 0.28 유지, 캘리브레이션 런은 config에 명시 |
| 클러스터 pH (GEMS) | 12.4–13.9 — **주 용액**(액체 부피 1% 이상 클러스터)에만 판정(rev.2). 미량수 포켓은 알려진 진단 노이즈(§4.5)라 이탈 개수·범위만 집계해 value에 표시. **v3.0/E3 주의**: 알칼리 담체를 넣으면 pH가 실제로 올라간다(무알칼리 런은 알칼리가 없어 이 밴드를 아래쪽에서 만족했을 뿐이다). 이 밴드는 무알칼리 기준이므로 알칼리 런의 이탈은 밴드 부적합 신호이지 코드 결함 신호가 아니다 — Na2O당량별 문헌 밴드(Lothenbach류 측정)를 확보하기 전까지 상향하지 않고, `binder_oxides.na2o_eq_pct`를 함께 읽어 해석한다 |

### 6.4 하지 않는 것

QXRD/TGA/XCT 데이터셋 파이프라인, 캘리브레이션/홀드아웃 절차, 불확도 전파, 해상도 앙상블 연구,
evidence registry. 이것들은 v2 완료 후 별도 프로젝트로 다룬다.

---

## 7. Fable 재구현 실행 계획

1. **새 저장소에서 시작한다.** v1 코드는 참조하되 복사하지 않는다(참조 경로:
   `C:\Users\solmo\TINN platform`). 특히 v1의 `pk_kinetics.py`, `storage.py`, `transport.py`는
   알고리즘 참조 가치가 높다.
2. 세션 구성: 마일스톤당 1개 세션을 기본으로 하고, 각 세션 시작 시 이 PRD 전문과
   직전 마일스톤 커밋 로그를 컨텍스트로 준다.
3. 각 세션 지시문 템플릿: "PRD §3의 M_n을 구현하라. DoD의 스모크 런과 테스트만 작성하고,
   §0의 설계 원칙(모듈 상한 16, 테스트 예산, 문서 2개)을 위반하지 마라."
4. 리뷰 게이트: 각 마일스톤 커밋 후 `/code-review` 1회, 발견된 correctness 이슈만 수정.
5. GEMS PC 번들(`PC-dat.lst` 외 4파일)과 xGEMS 설치는 M3 시작 전에 사용자가 새 저장소에
   복사·확인한다. 미준비 시 M3–M4를 보류하고 M5를 합성 백엔드 산출물로 선진행한다.

## 8. 성공 기준 (v2 전체)

- [ ] M0–M5 전체 DoD 통과, 총 모듈 ≤ 16, 총 테스트 ≤ §0 규칙 4의 현행 예산
      (v2 작성 당시 150 → v2.2 rev.2 180 → v3.0/E1·E2 220 → v3.0/E3 240).
- [ ] 합성 백엔드만으로: 4상 OPC 32³ 런 + 재시작 + 리포트가 GEMS 설치 없이 동작.
- [ ] GEMS 백엔드로: OPC 32³ 런이 1일 이상 시뮬레이션 시간을 완주하고 §6.3 리포트 생성.
- [ ] 신규 사용자가 README만으로 30분 내 첫 런 재현.
- [ ] 이 PRD가 코드와 일치 (불일치 발견 시 코드가 아니라 PRD를 먼저 갱신했는지 확인).

### v4.0/RT 성공 기준 (rt-dev)

- [ ] `transport` config 부재 런 = 현행 엔진 (레거시 config_hash 불변 + RT-W0 앵커 재현).
- [ ] τ ≤ dt 이중 런과 1-도메인(타일=격자) 이중 런이 각각 모드 부재 런과 full_hash 비트 동일.
- [ ] 모드 C 봉인 런에서 §6.1 전 항목(교환 수지 포함) 폐합, 결정론 재현, 경제 상태 포함
      재시작 비트 동일.
- [ ] RT 해석 앵커 1·2 통과 (GEMS 불요 테스트).
- [ ] RT-W3: 무플럭스 경계 = 봉인 비트 동일, 비영 경계 플럭스가 폐합 항등식으로 정확 계상.
- [ ] 수송 연산자 오버헤드 < GEM 벽시계의 5% (스텝 시간 비율 기록).
